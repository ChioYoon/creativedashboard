# 유료 AI 인사이트 카드 구조화 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 유료 AI 인사이트 카드를 무료 카드와 동일 품질로 구조화(위닝/저효율 존 + 실행 플랜). 무료·프롬프트·데이터 불변.

**Architecture:** `js/gemini-api.js` 렌더 부품 공용화 → 유료 파서/렌더러 재사용 + `buildScoringSlotCards` 배선. `css/gemini-ui.css` 신규 클래스. `step1_integrated.html` 캐시 버전 쿼리.

**Tech Stack:** 바닐라 JS. preview + `preview_eval`(gemini-api.js 버전쿼리 없어 fresh-source `new Function` eval 또는 `?v=` 리로드).

## Global Constraints

- 무료 카드(`parseFreeWinning`/무료 winning 슬롯)·AI 프롬프트·호출 흐름·데이터 무변경. 유료 렌더 경로만 추가.
- 파싱 실패(`_ok:false`) → 기존 평평 `cardHtmlArr` 폴백.
- 공용 헬퍼: `_gaiChip`·`_gaiItemRow`(점수 칩 포함)·`_gaiCallout`. 무료 `_buildFreeWinningCard`는 이 헬퍼 위임(점수 없음 → 시각 동일).
- 배지색: 확장=expand(초록)·개선=improve(앰버)·중단=stop(빨강)·순위=rank(보라).

---

### Task 1: 유료 카드 구조화

**Files:** Modify `js/gemini-api.js`(962·996·1159 부근), `css/gemini-ui.css`(말미), `step1_integrated.html`(28-29).

- [ ] **Step 1: 공용 렌더 헬퍼 3종 추가**

`function _buildFreeWinningCard(parsed) {`(962) **앞**에 추가:
```js
// 공용 렌더 부품 (무료·유료 카드 공유). 점수 칩은 값 있을 때만(무료엔 없음 → 시각 동일).
function _gaiChip(label, val, cls) {
  return val ? `<span class="gai-chip ${cls}">${label} ${_esc(val)}</span>` : '';
}
function _gaiItemRow(it) {
  return `<div class="gai-item-row">
      <span class="gai-item-name">${_esc(it.name)}</span>
      ${_gaiChip('점수', it.score, 'gai-chip--score')}
      ${_gaiChip('IPM', it.ipm, 'gai-chip--ipm')}
      ${_gaiChip('CPA', it.cpa ? '₩' + it.cpa : '', 'gai-chip--cpa')}
      ${it.reason ? `<span class="gai-item-reason">${_esc(it.reason)}</span>` : ''}
    </div>`;
}
function _gaiCallout(c, cls) {
  return c ? `<div class="gai-callout ${cls}"><strong>${_esc(c.label)}</strong> · ${_esc(c.text)}</div>` : '';
}
```

- [ ] **Step 2: `_buildFreeWinningCard` 헬퍼 위임으로 교체**

`_buildFreeWinningCard`(962-994) 전체를 교체(로컬 chip/itemRow/calloutHtml 제거, 공용 헬퍼 사용):
```js
function _buildFreeWinningCard(parsed) {
  const winZone = `
    <div class="gai-zone gai-zone--win">
      <div class="gai-zone-header">🏆 위닝 소재</div>
      ${parsed.win.items.map(_gaiItemRow).join('')}
      ${_gaiCallout(parsed.win.callout, 'gai-callout--win')}
    </div>`;
  const loseZone = (parsed.lose.items.length || parsed.lose.callout) ? `
    <div class="gai-zone gai-zone--lose">
      <div class="gai-zone-header">⚠️ 저효율 소재</div>
      ${parsed.lose.items.map(_gaiItemRow).join('')}
      ${_gaiCallout(parsed.lose.callout, 'gai-callout--lose')}
    </div>` : '';
  const recoRow = parsed.recos.length ? `
    <div class="gai-reco-row">
      <span class="gai-reco-label">즉시 권고</span>
      ${parsed.recos.map(r => `<span class="gai-badge gai-badge--${r.kind === '확장' ? 'expand' : 'stop'}">${_esc(r.kind)} · ${_esc(r.name)}</span>`).join('')}
    </div>` : '';
  return `<div class="gai-structured-card">${winZone}${loseZone}${recoRow}</div>`;
}
```

- [ ] **Step 3: 유료 파서 + 렌더러 추가**

`function parseScoringSlots(raw) {`(996) **앞**에 추가:
```js
// 유료 슬롯(winning/losing/actionItems/scaleUp/stopNow) → 구조 객체 (순수함수). 실패 시 _ok:false.
function parsePaidInsight(slots) {
  const norm = t => String(t == null ? '' : t).replace(/\\n/g, '\n');
  const ITEM_RE = /^[•\-▸▪◆]\s*\*\*(.+?)\*\*\s*(?:\(([^)]*)\))?\s*(?:[—:\-]\s*(.*))?$/;
  const CALLOUT_RE = /^\*\*(.+?)\*\*\s*[:：]\s*(.*)$/;
  const parseSection = (text) => {
    const items = [], callouts = [];
    for (const raw of norm(text).split('\n')) {
      const line = raw.trim();
      if (!line) continue;
      if (/^\*{0,2}\[/.test(line) && /소재\s*분석/.test(line.replace(/\*/g, ''))) continue;
      const im = line.match(ITEM_RE);
      if (im) {
        const m = im[2] || '';
        items.push({
          name: im[1].trim(),
          score: (m.match(/점수\s*([\d.]+)/) || [])[1] || null,
          ipm: (m.match(/IPM\s*([\d.]+)/i) || [])[1] || null,
          cpa: (m.match(/CPA\s*([\d,]+)/i) || [])[1] || null,
          reason: (im[3] || '').trim(),
        });
        continue;
      }
      const cm = line.match(CALLOUT_RE);
      if (cm) callouts.push({ label: cm[1].trim(), text: cm[2].trim() });
    }
    return { items, callouts };
  };
  const parseBullets = (text) => norm(text).split('\n').map(r => r.trim()).filter(Boolean)
    .map(line => {
      const m = line.match(/^[•\-▸▪◆]\s*(?:\*\*(.+?)\*\*)?\s*[—:\-]?\s*(.*)$/);
      return m ? { tag: (m[1] || '').trim(), text: (m[2] || '').trim() } : null;
    }).filter(b => b && (b.tag || b.text));
  const win = parseSection(slots.winning || '');
  const lose = parseSection(slots.losing || '');
  const actions = {
    immediate: parseBullets(slots.actionItems || ''),
    scaleUp: parseBullets(slots.scaleUp || ''),
    stopNow: parseBullets(slots.stopNow || ''),
  };
  return { win, lose, actions, _ok: win.items.length > 0 || lose.items.length > 0 };
}

function _buildPaidCard(parsed) {
  const zone = (cls, header, sec) => (sec.items.length || sec.callouts.length) ? `
    <div class="gai-zone ${cls}">
      <div class="gai-zone-header">${header}</div>
      ${sec.items.map(_gaiItemRow).join('')}
      ${sec.callouts.map(c => _gaiCallout(c, cls === 'gai-zone--win' ? 'gai-callout--win' : 'gai-callout--lose')).join('')}
    </div>` : '';
  const badgeCls = (tag) => {
    const t = tag || '';
    if (/확장/.test(t)) return 'gai-badge--expand';
    if (/개선/.test(t)) return 'gai-badge--improve';
    if (/중단/.test(t)) return 'gai-badge--stop';
    return 'gai-badge--rank';
  };
  const actionRow = (b) => `<div class="gai-action-row">
      ${b.tag ? `<span class="gai-badge ${badgeCls(b.tag)}">${_esc(b.tag)}</span>` : ''}
      ${b.text ? `<span class="gai-action-text">${_esc(b.text)}</span>` : ''}
    </div>`;
  const group = (icon, title, bullets) => bullets.length ? `
    <div class="gai-action-group">
      <div class="gai-action-title">${icon} ${title}</div>
      ${bullets.map(actionRow).join('')}
    </div>` : '';
  const hasActions = parsed.actions.immediate.length || parsed.actions.scaleUp.length || parsed.actions.stopNow.length;
  const actionZone = hasActions ? `
    <div class="gai-zone gai-zone--action">
      ${group('⚡', '즉시 실행', parsed.actions.immediate)}
      ${group('🚀', '스케일업', parsed.actions.scaleUp)}
      ${group('🛑', '중단 권고', parsed.actions.stopNow)}
    </div>` : '';
  return `<div class="gai-structured-card">
    ${zone('gai-zone--win', '🏆 위닝 소재', parsed.win)}
    ${zone('gai-zone--lose', '⚠️ 저효율 소재', parsed.lose)}
    ${actionZone}
  </div>`;
}
```

- [ ] **Step 4: `buildScoringSlotCards` 배선**

`isWinningOnly` 폴백 주석(`// _ok:false → 아래 기존 cardHtmlArr 경로로 폴백`, 1158) 다음 `}`(1159) 뒤, `const cardHtmlArr`(1161) 앞에 추가:
```js
  // 유료(다중 슬롯): 구조화 렌더 시도, 실패 시 아래 평평 렌더로 폴백
  if (!isWinningOnly && (slots.winning || slots.losing)) {
    const parsedPaid = parsePaidInsight(slots);
    if (parsedPaid._ok) {
      return `${_buildPaidCard(parsedPaid)}
        <div class="gai-slot-summary"><span>${summaryParts.join(' ')}</span>${retryBtn}</div>`;
    }
  }
```

- [ ] **Step 5: CSS 신규 클래스**

`css/gemini-ui.css` 말미(마지막 `.gai-badge--stop` 규칙 뒤)에 추가:
```css
.gai-chip--score { color: #dc2626; font-weight: 600; }
.gai-zone--action { background: #faf5ff; border-top: 1px solid #e5e7eb; }
.gai-action-group { margin-bottom: 10px; }
.gai-action-group:last-child { margin-bottom: 0; }
.gai-action-title { font-size: 12.5px; font-weight: 700; color: #6b21a8; margin-bottom: 6px; }
.gai-action-row { display: flex; align-items: baseline; flex-wrap: wrap; gap: 6px; margin-bottom: 5px; font-size: 12.5px; line-height: 1.5; }
.gai-action-text { color: #374151; }
.gai-badge--improve { background: #fffbeb; color: #d97706; }
.gai-badge--rank { background: #f5f3ff; color: #8b5cf6; }
```

- [ ] **Step 6: 캐시 버전 쿼리 (step1)**

`step1_integrated.html`(28-29) 교체:
```html
  <link rel="stylesheet" href="css/gemini-ui.css?v=20260701a">
  <script src="js/gemini-api.js?v=20260701a"></script>
```

- [ ] **Step 7: preview 검증 — 유료 구조화·무료 무회귀·폴백**

`step1_integrated.html?v=20260701a&_=<ts>` 로드 후 `preview_eval`:
```js
(function(){
  var paidSlots = {
    winning: '**[위닝 소재 분석]**\n• **A소재** (점수 90.1 / IPM 5.200 / CPA 3,000) — 강한 임팩트\n**공통 패턴**: 캐릭터 전면 — 후킹 강함',
    losing:  '**[저효율 소재 분석]**\n• **B소재** (점수 20.0 / IPM 0.500 / CPA 9,000) — 밋밋\n**위닝 대비 차이점**: 임팩트 부족\n**개선 방향**: 첫 3초 강화',
    actionItems: '• **[확장 🟢]** A소재 — IPM 5.2\n• **[개선 🟡]** C소재 — CTA 추가\n• **[중단 🔴]** B소재 — CPA 과다',
    scaleUp: '• **1순위** A소재 — 캐릭터 태그\n• 확장 시 CPA 개선 기대',
    stopNow: '• **중단** B소재 — 저효율\n• 교체: 임팩트형 신규'
  };
  var paidHtml = buildScoringSlotCards(paidSlots);
  // 무료 무회귀
  var freeHtml = buildScoringSlotCards({ winning: '**[위닝 소재]**\n• **A소재** (IPM 5.2 / CPA 3,000) — 이유\n**공통 패턴**: X\n**[저효율 소재]**\n• **B소재** (IPM 0.5 / CPA 9,000) — 원인\n**위닝 대비 차이**: Y\n**즉시 권고**: [확장 🟢] A소재 / [중단 🔴] B소재' });
  // 폴백
  var fallbackHtml = buildScoringSlotCards({ winning: '자유 텍스트, 형식 없음', losing: '' });
  return {
    paid_structured: paidHtml.indexOf('gai-structured-card') !== -1,
    paid_winZone: paidHtml.indexOf('gai-zone--win') !== -1,
    paid_loseZone: paidHtml.indexOf('gai-zone--lose') !== -1,
    paid_actionZone: paidHtml.indexOf('gai-zone--action') !== -1,
    paid_scoreChip: paidHtml.indexOf('gai-chip--score') !== -1,
    paid_expandBadge: paidHtml.indexOf('gai-badge--expand') !== -1,
    paid_improveBadge: paidHtml.indexOf('gai-badge--improve') !== -1,
    paid_stopBadge: paidHtml.indexOf('gai-badge--stop') !== -1,
    free_structured: freeHtml.indexOf('gai-structured-card') !== -1,
    free_noScoreChip: freeHtml.indexOf('gai-chip--score') === -1,
    fallback_flat: fallbackHtml.indexOf('gai-slot-card') !== -1 || fallbackHtml.indexOf('인사이트 데이터가 없습니다') !== -1
  };
})()
```
Expected: paid_* 전부 true, free_structured:true, free_noScoreChip:true, fallback_flat:true. `preview_console_logs`(error) → 없음. `preview_screenshot`로 유료 카드 시각 확인(합성 렌더 삽입).

- [ ] **Step 8: Commit**
```bash
git add js/gemini-api.js css/gemini-ui.css step1_integrated.html
git commit -m "feat(ai): 유료 인사이트 카드 구조화 — 위닝/저효율 존 + 실행 플랜"
```

---

## 실행 메모
- 인라인 JS/CSS → pytest 무관. 검증은 buildScoringSlotCards 합성 호출.
- `gemini-api.js`는 버전쿼리 추가 전 브라우저 캐시 가능 → 검증 시 `?v=20260701a` 리로드 또는 fresh-source eval.
- 무료 무회귀는 `gai-chip--score` 부재 + 구조 동일로 확인.
