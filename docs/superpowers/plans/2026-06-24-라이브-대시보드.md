# 라이브 대시보드 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 노출 중인 소재를 한 화면에서 — 상단 필터(타이틀·기간·국가·OS·캠페인) + 중단 통합 추이 그래프(Top N) + 하단 전체 소재 그리드(클릭 시 분석 모달) — 으로 조회하는 신규 독립 페이지를 만든다.

**Architecture:** step1/step2와 동일한 독립 HTML(`live_dashboard.html`) + 전용 로직 모듈(`js/live-dashboard.js`). 데이터는 `public/data/{title}.json`의 `creatives` 배열(평탄)을 직접 fetch해 소비. 국가/OS는 `campaign_name` 파싱. 그래프는 Chart.js, 통합 집계(Google Ads 전환 + MMP 설치). 모달은 step1 무회귀를 위해 순수 헬퍼(`buildSignalChipsHtml`/`buildInsightBlocksHtml`)를 모듈에 **자체 복제**(step1 미변경).

**Tech Stack:** 정적 HTML + 바닐라 JS, Chart.js 4.4.0(CDN, step1과 동일), `js/data-source.js`(매니페스트), `assets/cl-shell.css`(공용 헤더). JS 단위 테스트 하니스 없음 → 검증은 preview MCP 도구.

## Global Constraints

- 파이프라인·스코어·태깅·캐시·`step1_integrated.html`·`step2_*` **로직 변경 금지** (헤더 네비 링크 1줄 추가만 예외).
- **step1 무회귀가 하드 제약** — 모달 헬퍼는 step1에서 추출하지 않고 모듈에 복제(step1 미변경).
- **그리드는 노출 유무 무관 전체 소재 표시** — 필터는 그래프만 스코프, 그리드 카드는 숨기지 않음.
- 그래프 = **통합 단일 곡선**: 일별 Google Ads `conversions` + MMP `installs` = 획득, `impressions` 합산. (MMP가 Google 채널 제외 → 비중복)
- Top N = **획득 상위** (기본 7, 5~10 조절).
- 국가/OS는 `campaign_name` 파싱, 토큰 없으면 '미상'.
- KPI 미연동 타이틀 → **그래프 영역만** "데이터 없음" 안내, 그리드+모달은 동작.
- DS 토큰 사용(`--brand-primary` #DC2828 등), 무이모지 원칙(기능기호 📷/▶/📁 Drive 예외).
- 페이지명/네비 라벨 = **"라이브 대시보드"**, 파일 = `live_dashboard.html`.

---

## 파일 맵

| 파일 | 책임 |
|------|------|
| `live_dashboard.html` (신규) | 페이지 구조 — cl-shell 헤더, 필터 바, 그래프 캔버스, 그리드 컨테이너, 모달 컨테이너. script 태그 로드. |
| `js/live-dashboard.js` (신규) | 전 로직 — 타이틀 로드, campaign 파서, 필터, 통합 집계, Chart.js, 그리드, 모달. |
| `index.html`·`step1_integrated.html`·`step2_column_selector.html`·`step2_clustering.html` (수정) | `.cl-nav`에 "라이브 대시보드" 링크 1줄 추가 |

**데이터(직접 소비, raw flat):** `public/data/{title}.json` → `data.creatives[]`. 각 creative 최상위 필드: `소재명·유형·파일명·링크·one_line_insight·creator_intent·strengths·strength_evidence·weaknesses·weakness_evidence·hypothesis·test_ideas·improvement_actions·kpi_reality_check·mmp_*·source_files·kpi_daily[]·mmp_daily[]`.
- `kpi_daily[]` 항목: `{date, campaign_name, impressions, clicks, cost, conversions, ...}`
- `mmp_daily[]` 항목: `{date, channel, campaign_name, impressions, clicks, cost, installs, retained_d1, revenue_d7}`

---

## Task 1: 페이지 골격 + 헤더 + 타이틀 로드 + KPI 미연동 graceful

**Files:**
- Create: `live_dashboard.html`
- Create: `js/live-dashboard.js`
- Modify: `index.html`, `step1_integrated.html`, `step2_column_selector.html`, `step2_clustering.html` (네비 1줄)

**Interfaces — Produces:**
- 전역 `LIVE.state = { titleId, creatives: [], driveFolderUrl: '', hasKpi: bool }`
- `LIVE.loadTitle(titleId)` — `public/data/{titleId}.json` fetch → state 채움 → `LIVE.render()`
- `LIVE.render()` — (Task 2~4에서 확장) 현재 빈 골격 렌더 + KPI 미연동 시 그래프 안내

---

- [ ] **Step 1: `live_dashboard.html` 골격 생성**

`assets/cl-shell.css` 헤더 패턴(step2_clustering.html 696-708 참고)을 따르되 네비 active를 "라이브 대시보드"로. 구조:

```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
  <meta http-equiv="Pragma" content="no-cache"><meta http-equiv="Expires" content="0">
  <title>라이브 대시보드 - Com2uS R팀</title>
  <link rel="stylesheet" href="assets/cl-shell.css">
  <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.js"></script>
  <style>
    /* 페이지 전용 — DS 토큰 사용. 풀블리드 surface 배경 */
    body { margin:0; background: var(--surface-soft, #FAF9F7); font-family: var(--font-base, 'Noto Sans KR', 'Pretendard', sans-serif); color: var(--text-primary, #1f2937); }
    .live-body { max-width: 1280px; margin: 0 auto; padding: 20px; }
    .live-filters { display:flex; gap:12px; flex-wrap:wrap; align-items:flex-end; background:#fff; border:1px solid var(--border-default,#e5e7eb); border-radius:10px; padding:14px 16px; margin-bottom:16px; }
    .live-filter-group { display:flex; flex-direction:column; gap:4px; font-size:12px; }
    .live-filter-group label { font-weight:600; color: var(--text-secondary,#6b7280); }
    .live-chart-card { background:#fff; border:1px solid var(--border-default,#e5e7eb); border-radius:10px; padding:16px; margin-bottom:16px; min-height:280px; }
    .live-chart-toolbar { display:flex; gap:8px; align-items:center; margin-bottom:12px; flex-wrap:wrap; }
    .live-metric-btn { cursor:pointer; padding:6px 14px; border:1px solid var(--border-default,#e5e7eb); border-radius:20px; background:#fff; font-size:12px; font-weight:600; }
    .live-metric-btn.active { background: var(--brand-primary,#DC2828); color:#fff; border-color: var(--brand-primary,#DC2828); }
    .live-empty { padding:48px 24px; text-align:center; color:#9ca3af; font-size:13px; }
    .live-grid { display:grid; grid-template-columns: repeat(auto-fill, minmax(180px, 1fr)); gap:14px; }
    .live-card { background:#fff; border:1px solid var(--border-default,#e5e7eb); border-radius:10px; overflow:hidden; cursor:pointer; transition:box-shadow .15s; }
    .live-card:hover { box-shadow:0 4px 14px rgba(0,0,0,.08); }
    .live-card-preview { width:100%; aspect-ratio:16/10; background:#F4F2EF; display:flex; align-items:center; justify-content:center; overflow:hidden; }
    .live-card-preview img { width:100%; height:100%; object-fit:cover; }
    .live-card-body { padding:8px 10px; }
    .live-card-name { font-size:12px; font-weight:600; word-break:break-all; line-height:1.35; }
    .live-card-badge { font-size:11px; color: var(--text-secondary,#6b7280); margin-top:4px; }
    /* 모달 (Task 4에서 내용 렌더) */
    .live-modal { display:none; position:fixed; inset:0; background:rgba(0,0,0,.5); z-index:1000; align-items:flex-start; justify-content:center; overflow:auto; padding:40px 16px; }
    .live-modal.active { display:flex; }
    .live-modal-box { background:#fff; border-radius:14px; max-width:680px; width:100%; padding:24px; position:relative; }
    .live-modal-close { position:absolute; top:14px; right:16px; cursor:pointer; font-size:22px; color:#9ca3af; background:none; border:none; }
  </style>
</head>
<body>
  <header class="cl-topbar">
    <a href="index.html" class="cl-brand">
      <img src="assets/com2us/logo-primary-red.png" alt="Com2uS" class="cl-brand-logo">
      <span class="cl-brand-divider"></span>
      <span class="cl-brand-title">소재 분석 대시보드</span>
    </a>
    <nav class="cl-nav">
      <a href="index.html" class="cl-nav-link">홈</a>
      <a href="step1_integrated.html" class="cl-nav-link">Step 1 소재 분석</a>
      <a href="step2_column_selector.html" class="cl-nav-link">Step 2 군집화</a>
      <a href="live_dashboard.html" class="cl-nav-link active">라이브 대시보드</a>
      <a href="사용안내서_인쇄용.html" target="_blank" class="cl-nav-link">사용 안내서</a>
    </nav>
  </header>

  <div class="live-body">
    <div class="live-filters" id="liveFilters">
      <div class="live-filter-group">
        <label>타이틀</label>
        <select id="liveTitleSelect"></select>
      </div>
      <!-- 기간·국가·OS·캠페인 필터는 Task 2에서 주입 -->
      <div id="liveFilterDynamic" style="display:flex; gap:12px; flex-wrap:wrap; align-items:flex-end;"></div>
    </div>

    <div class="live-chart-card">
      <div class="live-chart-toolbar" id="liveChartToolbar">
        <strong style="font-size:13px;">주요 소재 추이 (획득 상위)</strong>
        <span style="flex:1;"></span>
        <button class="live-metric-btn active" data-metric="impr">노출</button>
        <button class="live-metric-btn" data-metric="acq">획득</button>
        <button class="live-metric-btn" data-metric="cpi">CPI</button>
        <label style="font-size:12px; margin-left:8px;">Top <select id="liveTopN"><option>5</option><option selected>7</option><option>10</option></select></label>
      </div>
      <div id="liveChartArea"><canvas id="liveChart" height="120"></canvas></div>
    </div>

    <div class="live-grid" id="liveGrid"></div>
  </div>

  <div class="live-modal" id="liveModal"><div class="live-modal-box"><button class="live-modal-close" id="liveModalClose">✕</button><div id="liveModalBody"></div></div></div>

  <script src="js/data-source.js?v=20260624a"></script>
  <script src="js/layer-metrics.js?v=20260624a"></script>
  <script src="js/live-dashboard.js?v=20260624a"></script>
</body>
</html>
```

- [ ] **Step 2: `js/live-dashboard.js` 골격 — 타이틀 로드 + KPI 감지**

```javascript
/* 라이브 대시보드 — 노출 중 소재 그리드 + 통합 추이 그래프 */
(function () {
  'use strict';
  const LIVE = {
    state: { titleId: '', creatives: [], driveFolderUrl: '', hasKpi: false, manifest: [] },
    metric: 'impr',
    topN: 7,
    chart: null,
  };
  window.LIVE = LIVE;

  function el(id) { return document.getElementById(id); }

  async function init() {
    // 타이틀 매니페스트 → 셀렉터
    LIVE.state.manifest = await window.DataSource.loadTitleManifest();
    const sel = el('liveTitleSelect');
    LIVE.state.manifest
      .filter(t => t.json_url)            // 데이터 있는 타이틀만
      .forEach(t => {
        const o = document.createElement('option');
        o.value = t.id; o.textContent = t.name; sel.appendChild(o);
      });
    sel.addEventListener('change', () => LIVE.loadTitle(sel.value));
    // URL ?title= 또는 첫 항목 자동 로드
    const urlTitle = window.DataSource.readTitleFromUrl();
    const start = (urlTitle && LIVE.state.manifest.some(t => t.id === urlTitle)) ? urlTitle
                : (sel.options[0] && sel.options[0].value) || '';
    if (start) { sel.value = start; await LIVE.loadTitle(start); }
  }

  LIVE.loadTitle = async function (titleId) {
    if (!titleId) return;
    const meta = LIVE.state.manifest.find(t => t.id === titleId) || {};
    const res = await fetch(`public/data/${titleId}.json`, { cache: 'no-store' });
    const data = res.ok ? await res.json() : { creatives: [] };
    const creatives = data.creatives || [];
    LIVE.state.titleId = titleId;
    LIVE.state.creatives = creatives;
    LIVE.state.driveFolderUrl = meta.drive_folder_url || '';
    // KPI 미연동 감지 — step1과 동일 기준
    LIVE.state.hasKpi = creatives.some(c =>
      (c.kpi_daily && c.kpi_daily.length) || (c.mmp_daily && c.mmp_daily.length));
    LIVE.render();
  };

  LIVE.render = function () {
    // Task 2~4에서 확장. 현재: KPI 미연동 시 그래프 영역 안내, 그리드는 Task 4.
    const chartArea = el('liveChartArea');
    if (!LIVE.state.hasKpi) {
      chartArea.innerHTML = '<div class="live-empty">이 타이틀은 운영(KPI) 데이터가 없어 추이를 표시할 수 없습니다. 아래 소재 목록과 분석은 확인할 수 있습니다.</div>';
    } else {
      chartArea.innerHTML = '<canvas id="liveChart" height="120"></canvas>';
    }
  };

  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', init);
  else init();
})();
```

- [ ] **Step 3: 네 페이지 네비에 "라이브 대시보드" 추가**

`index.html`·`step1_integrated.html`·`step2_column_selector.html`·`step2_clustering.html` 각각의 `.cl-nav` 안, "Step 2 군집화" 링크 **뒤**에 한 줄 추가(active 없이):

```html
      <a href="live_dashboard.html" class="cl-nav-link">라이브 대시보드</a>
```

각 파일에서 찾을 패턴(파일마다 active 위치는 다르나 군집화 링크는 공통):
```html
      <a href="step2_column_selector.html" class="cl-nav-link...">Step 2 군집화</a>
```
바로 다음 줄에 위 링크 삽입. (step1은 `step2Link` id가 있을 수 있으니 그 줄 다음에 삽입.)

- [ ] **Step 4: preview 검증 — 로드 + KPI 미연동 graceful**

preview 도구:
1. `preview_start`(이미 실행 중이면 재사용), `live_dashboard.html` 로 이동
2. `preview_eval`: `(LIVE.state.manifest||[]).length` → 기대: ≥2
3. `?title=pepp-us` 로드 후 `preview_eval`: `JSON.stringify({n: LIVE.state.creatives.length, kpi: LIVE.state.hasKpi, drive: !!LIVE.state.driveFolderUrl})` → 기대: n>0, kpi=true, drive=true
4. 타이틀을 tougenanki로 변경 후 `preview_eval`: `JSON.stringify({n: LIVE.state.creatives.length, kpi: LIVE.state.hasKpi})` → 기대: n>0(28), kpi=false
5. `preview_snapshot` — 헤더 "라이브 대시보드" active + tougenanki에서 그래프 영역 "데이터 없음" 안내
6. `preview_console_logs` level error → 없음

- [ ] **Step 5: 커밋**

```bash
cd C:\claude\cloop_dashboard
git add live_dashboard.html js/live-dashboard.js index.html step1_integrated.html step2_column_selector.html step2_clustering.html
git commit -m "feat(live): 라이브 대시보드 골격 + 타이틀 로드 + KPI 미연동 graceful + 네비"
```

---

## Task 2: campaign_name 파서 + 필터 바

**Files:**
- Modify: `js/live-dashboard.js`, `live_dashboard.html`(필터 동적 영역은 JS가 주입)

**Interfaces — Consumes:** `LIVE.state.creatives` (Task 1)
**Interfaces — Produces:**
- `LIVE.parseCountry(campaignName) -> string` (없으면 `'미상'`)
- `LIVE.parseOS(campaignName) -> string` (없으면 `'미상'`)
- `LIVE.state.filters = { start, end, countries:Set, oses:Set, campaigns:Set }`
- `LIVE.allDailies()` — 모든 creative의 `kpi_daily`+`mmp_daily`를 `{creative, date, campaign_name, country, os, src, impr, acq, cost}` 평탄 배열로
- `LIVE.applyFilters()` — 필터된 dailies 반환

---

- [ ] **Step 1: 파서 함수 추가** (`js/live-dashboard.js` 내부)

```javascript
  // campaign_name 예: HQ_HQ_PH_US-EN_GA_NU_AD_ACA-PU_260429 / HQ_HQ_PH_CA-EN_FB_NU_iOS_INSTALL_260122
  LIVE.parseCountry = function (cn) {
    if (!cn) return '미상';
    const seg = String(cn).split('_').find(s => /^[A-Z]{2,4}-[A-Z]{2}$/.test(s));
    return seg ? seg.split('-')[0] : '미상';
  };
  const OS_TOKENS = { ios: 'iOS', aos: 'Android', android: 'Android', web: 'Web' };
  LIVE.parseOS = function (cn) {
    if (!cn) return '미상';
    for (const s of String(cn).split('_')) {
      const k = s.toLowerCase();
      if (OS_TOKENS[k]) return OS_TOKENS[k];
    }
    return '미상';
  };
```

- [ ] **Step 2: dailies 평탄화 + 필터 적용**

```javascript
  // 모든 일별 엔트리를 통합 형태로 평탄화 (Google Ads=conversions, MMP=installs → acq)
  LIVE.allDailies = function () {
    const out = [];
    for (const c of LIVE.state.creatives) {
      for (const k of (c.kpi_daily || [])) {
        out.push({ creative: c, date: k.date, campaign_name: k.campaign_name,
          country: LIVE.parseCountry(k.campaign_name), os: LIVE.parseOS(k.campaign_name),
          src: 'GA', impr: +k.impressions || 0, acq: +k.conversions || 0, cost: +k.cost || 0 });
      }
      for (const m of (c.mmp_daily || [])) {
        out.push({ creative: c, date: m.date, campaign_name: m.campaign_name,
          country: LIVE.parseCountry(m.campaign_name), os: LIVE.parseOS(m.campaign_name),
          src: 'MMP', impr: +m.impressions || 0, acq: +m.installs || 0, cost: +m.cost || 0 });
      }
    }
    return out;
  };

  LIVE.applyFilters = function () {
    const f = LIVE.state.filters;
    return LIVE.allDailies().filter(d => {
      if (f.start && d.date < f.start) return false;
      if (f.end && d.date > f.end) return false;
      if (f.countries.size && !f.countries.has(d.country)) return false;
      if (f.oses.size && !f.oses.has(d.os)) return false;
      if (f.campaigns.size && !f.campaigns.has(d.campaign_name)) return false;
      return true;
    });
  };
```

- [ ] **Step 3: 필터 바 UI 주입 + 옵션 도출 + 이벤트**

`LIVE.buildFilters()` — `allDailies()`에서 distinct 국가/OS/캠페인·날짜범위 도출 후 `#liveFilterDynamic`에 멀티선택 체크박스 그룹 + 날짜 프리셋 버튼 렌더. 변경 시 `LIVE.state.filters` 갱신 후 `LIVE.render()`. (날짜 프리셋: 전체/최근7/14/28일 — 데이터 최대일 기준 역산.) `LIVE.loadTitle` 끝에서 `LIVE.buildFilters()` 호출(필터 초기화=전체). 초기 `filters = {start:'', end:'', countries:new Set(), oses:new Set(), campaigns:new Set()}`(빈 Set=전체).

```javascript
  LIVE.buildFilters = function () {
    const ds = LIVE.allDailies();
    const countries = [...new Set(ds.map(d => d.country))].sort();
    const oses = [...new Set(ds.map(d => d.os))].sort();
    const camps = [...new Set(ds.map(d => d.campaign_name).filter(Boolean))].sort();
    const dates = ds.map(d => d.date).filter(Boolean).sort();
    LIVE.state._maxDate = dates[dates.length - 1] || '';
    LIVE.state.filters = { start:'', end:'', countries:new Set(), oses:new Set(), campaigns:new Set() };
    const host = el('liveFilterDynamic');
    if (!ds.length) { host.innerHTML = '<span style="font-size:12px;color:#9ca3af;">운영 데이터 없음 — 필터 비활성 (아래 소재 목록은 전체 표시)</span>'; return; }
    const presetBtns = [['전체',0],['최근 7일',7],['14일',14],['28일',28]]
      .map(([lbl,n]) => `<button class="live-metric-btn" data-days="${n}">${lbl}</button>`).join('');
    const checks = (name, arr) => arr.map(v =>
      `<label style="font-size:12px;display:inline-flex;gap:4px;align-items:center;margin-right:8px;"><input type="checkbox" data-filter="${name}" value="${v}">${v}</label>`).join('');
    host.innerHTML =
      `<div class="live-filter-group"><label>기간</label><div>${presetBtns}</div></div>` +
      `<div class="live-filter-group"><label>국가</label><div>${checks('countries', countries)}</div></div>` +
      `<div class="live-filter-group"><label>OS</label><div>${checks('oses', oses)}</div></div>` +
      `<div class="live-filter-group" style="max-width:320px;"><label>캠페인</label><div style="max-height:60px;overflow:auto;">${checks('campaigns', camps)}</div></div>`;
    host.querySelectorAll('input[type="checkbox"]').forEach(cb => cb.addEventListener('change', () => {
      const set = LIVE.state.filters[cb.dataset.filter];
      cb.checked ? set.add(cb.value) : set.delete(cb.value);
      LIVE.render();
    }));
    host.querySelectorAll('button[data-days]').forEach(b => b.addEventListener('click', () => {
      host.querySelectorAll('button[data-days]').forEach(x => x.classList.remove('active'));
      b.classList.add('active');
      const n = +b.dataset.days;
      if (!n || !LIVE.state._maxDate) { LIVE.state.filters.start=''; LIVE.state.filters.end=''; }
      else { const end = LIVE.state._maxDate; const d = new Date(end); d.setDate(d.getDate()-(n-1));
        LIVE.state.filters.start = d.toISOString().slice(0,10); LIVE.state.filters.end = end; }
      LIVE.render();
    }));
  };
```
`LIVE.loadTitle` 끝(`LIVE.render()` 앞)에 `LIVE.buildFilters();` 추가.

- [ ] **Step 4: preview 검증 — 필터 옵션**

1. `?title=pepp-us` 로드 후 `preview_eval`:
   ```js
   (function(){var ds=LIVE.allDailies();return JSON.stringify({countries:[...new Set(ds.map(d=>d.country))].sort(), oses:[...new Set(ds.map(d=>d.os))].sort(), camps:[...new Set(ds.map(d=>d.campaign_name))].length});})()
   ```
   기대: countries에 `US`·`CA`·`SEA`·`PH` 포함, oses에 `iOS`·`미상` 포함, camps ≥ 10
2. `preview_eval`: `LIVE.parseCountry('HQ_HQ_PH_US-EN_GA_NU_AD_ACA-PU_260429')` → `"US"`; `LIVE.parseOS('HQ_HQ_PH_CA-EN_FB_NU_iOS_INSTALL_260122')` → `"iOS"`; `LIVE.parseOS('HQ_HQ_PH_US-EN_GA_NU_AD_ACA-PU_260429')` → `"미상"`
3. `preview_snapshot` — 필터 바에 국가/OS/캠페인 체크박스 + 기간 버튼

- [ ] **Step 5: 커밋**

```bash
cd C:\claude\cloop_dashboard
git add js/live-dashboard.js
git commit -m "feat(live): campaign_name 국가/OS 파서 + 필터 바(기간/국가/OS/캠페인)"
```

---

## Task 3: 통합 일별 집계 + Top N 추이 그래프 (Chart.js)

**Files:**
- Modify: `js/live-dashboard.js`

**Interfaces — Consumes:** `LIVE.applyFilters()`, `LIVE.state.metric/topN` (Task 1·2)
**Interfaces — Produces:**
- `LIVE.aggregate(dailies)` → `{ perCreative: Map(creative→{impr,acq,cost,byDate:Map(date→{impr,acq,cost})}), dates: string[] }`
- `LIVE.renderChart()` — 획득 상위 Top N 소재의 선택 지표 라인

---

- [ ] **Step 1: 집계 함수**

```javascript
  LIVE.aggregate = function (dailies) {
    const perCreative = new Map();   // creative → {impr,acq,cost, byDate:Map}
    const dateSet = new Set();
    for (const d of dailies) {
      dateSet.add(d.date);
      let e = perCreative.get(d.creative);
      if (!e) { e = { impr:0, acq:0, cost:0, byDate:new Map() }; perCreative.set(d.creative, e); }
      e.impr += d.impr; e.acq += d.acq; e.cost += d.cost;
      let bd = e.byDate.get(d.date);
      if (!bd) { bd = { impr:0, acq:0, cost:0 }; e.byDate.set(d.date, bd); }
      bd.impr += d.impr; bd.acq += d.acq; bd.cost += d.cost;
    }
    return { perCreative, dates: [...dateSet].sort() };
  };
  // 지표값 추출 (CPI=cost/acq, 획득 0이면 null=gap)
  LIVE.metricVal = function (cell, metric) {
    if (!cell) return null;
    if (metric === 'impr') return cell.impr;
    if (metric === 'acq') return cell.acq;
    return cell.acq > 0 ? cell.cost / cell.acq : null;   // cpi
  };
```

- [ ] **Step 2: 차트 렌더**

```javascript
  const CHART_COLORS = ['#DC2828','#2563eb','#16a34a','#d97706','#7c3aed','#0891b2','#db2777','#65a30d','#ea580c','#4f46e5'];
  LIVE.renderChart = function () {
    const agg = LIVE.aggregate(LIVE.applyFilters());
    const area = el('liveChartArea');
    if (!agg.dates.length) { area.innerHTML = '<div class="live-empty">선택한 필터에 해당하는 추이 데이터가 없습니다.</div>'; LIVE.chart = null; return; }
    if (!area.querySelector('canvas')) area.innerHTML = '<canvas id="liveChart" height="120"></canvas>';
    // 획득 상위 Top N
    const top = [...agg.perCreative.entries()].sort((a,b) => b[1].acq - a[1].acq).slice(0, LIVE.topN);
    const datasets = top.map(([c, e], i) => ({
      label: c.소재명 || c.creative_id || ('소재'+i),
      data: agg.dates.map(dt => LIVE.metricVal(e.byDate.get(dt), LIVE.metric)),
      borderColor: CHART_COLORS[i % CHART_COLORS.length], backgroundColor: 'transparent',
      spanGaps: true, tension: 0.25, pointRadius: 2,
    }));
    if (LIVE.chart) LIVE.chart.destroy();
    LIVE.chart = new Chart(el('liveChart').getContext('2d'), {
      type: 'line',
      data: { labels: agg.dates, datasets },
      options: { responsive:true, maintainAspectRatio:false,
        plugins:{ legend:{ position:'bottom', labels:{ boxWidth:12, font:{ size:11 } } } },
        scales:{ y:{ beginAtZero:true } } },
    });
  };
```

- [ ] **Step 3: 지표 토글 + Top N 셀렉트 배선 + render 통합**

`init()` 안에 toolbar 이벤트 추가, `LIVE.render()`가 `renderChart()` 호출하도록.
```javascript
  function wireToolbar() {
    document.querySelectorAll('.live-metric-btn[data-metric]').forEach(b => b.addEventListener('click', () => {
      document.querySelectorAll('.live-metric-btn[data-metric]').forEach(x => x.classList.remove('active'));
      b.classList.add('active'); LIVE.metric = b.dataset.metric; LIVE.renderChart();
    }));
    el('liveTopN').addEventListener('change', e => { LIVE.topN = +e.target.value; LIVE.renderChart(); });
  }
```
`init()` 끝에 `wireToolbar();` 추가. `LIVE.render()`를 다음으로 교체:
```javascript
  LIVE.render = function () {
    if (!LIVE.state.hasKpi) { el('liveChartArea').innerHTML = '<div class="live-empty">이 타이틀은 운영(KPI) 데이터가 없어 추이를 표시할 수 없습니다. 아래 소재 목록과 분석은 확인할 수 있습니다.</div>'; }
    else { LIVE.renderChart(); }
    LIVE.renderGrid();   // Task 4
  };
```
(Task 4 전까지 `LIVE.renderGrid = function(){};` 임시 stub 추가.)

- [ ] **Step 4: preview 검증 — 그래프**

1. `?title=pepp-us` 로드 후 `preview_eval`: `(function(){return JSON.stringify({lines: LIVE.chart? LIVE.chart.data.datasets.length : 0, dates: LIVE.chart? LIVE.chart.data.labels.length : 0});})()` → 기대: lines = min(7, 소재수) > 0, dates > 0
2. 지표 토글: `preview_eval`로 `document.querySelector('.live-metric-btn[data-metric="acq"]').click()` 후 `LIVE.metric` → `"acq"`, 차트 재렌더(에러 없음)
3. `preview_snapshot` — 라인 차트 + 범례(소재명) + 노출/획득/CPI 버튼
4. `preview_console_logs` error → 없음

- [ ] **Step 5: 커밋**

```bash
cd C:\claude\cloop_dashboard
git add js/live-dashboard.js
git commit -m "feat(live): 통합 일별 집계 + 획득 상위 Top N 추이 그래프(Chart.js)"
```

---

## Task 4: 소재 그리드 + 분석 모달 + Drive 폴백

**Files:**
- Modify: `js/live-dashboard.js`

**Interfaces — Consumes:** `LIVE.state.creatives`, `LIVE.aggregate`, `LIVE.applyFilters` (Task 1·3)
**Interfaces — Produces:** `LIVE.renderGrid()`, `LIVE.openModal(creative)`

---

- [ ] **Step 1: 순수 헬퍼 복제(step1 미변경) + escapeHtml**

`js/live-dashboard.js`에 step1의 순수 헬퍼를 복제(`creative.meta.*` 읽음 → raw creative는 `{meta:c}`로 래핑해 호출):
```javascript
  function escapeHtml(s) { return String(s == null ? '' : s).replace(/[&<>"']/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m])); }

  function buildInsightBlocksHtml(creative) {
    const meta = (creative && creative.meta) || {};
    const insight = ((meta.one_line_insight || meta.marketer_insight) || '').trim();
    const reality = (meta.kpi_reality_check || '').trim();
    if (!insight && !reality) return '';
    const lead = insight ? `<div style="font-weight:600;margin:4px 0;">${escapeHtml(insight)}</div>` : '';
    const body = reality ? `<div style="color:#555;font-size:13px;">${escapeHtml(reality)}</div>` : '';
    return `<div style="border-left:3px solid var(--brand-primary,#DC2828);padding:8px 12px;background:#FAF9F7;border-radius:6px;margin:10px 0;"><span style="font-size:11px;font-weight:700;color:var(--brand-primary,#DC2828);">인사이트</span>${lead}${body}</div>`;
  }

  function buildSignalChipsHtml(creative) {
    const meta = (creative && creative.meta) || {};
    const groups = [
      { label:'강점', items:meta.strengths,  ev:meta.strength_evidence, color:'#16a34a' },
      { label:'약점', items:meta.weaknesses, ev:meta.weakness_evidence, color:'#d97706' },
      { label:'가설', items:meta.hypothesis, ev:null,                   color:'#2563eb' },
      { label:'차주 변주', items:meta.test_ideas, ev:meta.improvement_actions, color:'#6b7280', pfx:'→ ' },
    ];
    return groups.filter(g => Array.isArray(g.items) && g.items.length).map(g => {
      const cards = g.items.map((it, i) => {
        const e = ((g.ev && g.ev[i]) || '').trim();
        const eh = e ? `<div style="font-size:12px;color:#555;margin-top:2px;">${escapeHtml((g.pfx||'')+e)}</div>` : '';
        return `<div style="border-left:3px solid ${g.color};padding:6px 10px;margin:4px 0;background:#fff;"><div style="font-size:12.5px;font-weight:600;">${escapeHtml(it)}</div>${eh}</div>`;
      }).join('');
      return `<div style="margin:8px 0;"><div style="font-size:11px;font-weight:700;color:#6b7280;margin-bottom:2px;">${g.label}</div>${cards}</div>`;
    }).join('');
  }
```

- [ ] **Step 2: 미리보기 셀 + Drive 폴백**

```javascript
  function livePreviewHtml(c) {
    const url = c['링크'] || c.이미지링크 || '';
    const isVideo = (c.유형 || '').toUpperCase() === 'VID';
    const hasImg = url && url.startsWith('http') && !isVideo;   // ⚠️ 영상 링크는 유튜브 URL이라 <img> 부적합
    if (hasImg) return `<img src="${url}" alt="" style="width:100%;height:100%;object-fit:cover;">`;
    if (LIVE.state.driveFolderUrl) {
      const safe = LIVE.state.driveFolderUrl.replace(/'/g, "\\'");
      return `<button type="button" onclick="event.stopPropagation();window.open('${safe}','_blank','noopener')" title="공유 드라이브 폴더에서 소재 찾기" style="cursor:pointer;font-size:11px;font-weight:600;color:#666;background:#F4F2EF;border:1px solid #e5e0d8;border-radius:6px;padding:6px 8px;">📁 Drive</button>`;
    }
    return `<span style="color:#9ca3af;font-size:22px;">${isVideo ? '▶' : '📷'}</span>`;
  }
```

- [ ] **Step 3: 그리드 렌더 (전체 소재, 노출 무관)**

```javascript
  LIVE.renderGrid = function () {
    const agg = LIVE.aggregate(LIVE.applyFilters());   // 필터 기간 획득 배지용
    const acqOf = c => (agg.perCreative.get(c) || { acq:0 }).acq;
    // 전체 소재 — 노출 무관. 획득 상위순(무데이터 뒤로)
    const list = [...LIVE.state.creatives].sort((a, b) => acqOf(b) - acqOf(a));
    const grid = el('liveGrid');
    if (!list.length) { grid.innerHTML = '<div class="live-empty">이 타이틀에 소재가 없습니다.</div>'; return; }
    grid.innerHTML = list.map((c, i) => {
      const acq = acqOf(c);
      const badge = LIVE.state.hasKpi ? `<div class="live-card-badge">획득 ${acq.toLocaleString()}</div>` : '';
      return `<div class="live-card" data-idx="${i}"><div class="live-card-preview">${livePreviewHtml(c)}</div><div class="live-card-body"><div class="live-card-name">${escapeHtml(c.소재명 || c.creative_id || '')}</div>${badge}</div></div>`;
    }).join('');
    // 정렬된 list 를 클릭 핸들러에서 참조하도록 보관
    LIVE._gridList = list;
    grid.querySelectorAll('.live-card').forEach(card => card.addEventListener('click', () => {
      LIVE.openModal(LIVE._gridList[+card.dataset.idx]);
    }));
  };
```

- [ ] **Step 4: 분석 모달**

```javascript
  LIVE.openModal = function (c) {
    if (!c) return;
    const url = c['링크'] || c.이미지링크 || '';
    const hasUrl = url && url.startsWith('http');
    const isVideo = (c.유형 || '').toUpperCase() === 'VID';
    let preview;
    if (hasUrl && !isVideo) preview = `<img src="${url}" style="max-width:100%;border-radius:10px;" alt="">`;
    else if (hasUrl && isVideo) preview = `<div style="text-align:center;"><a href="${url}" target="_blank" rel="noopener" style="display:inline-block;padding:12px 20px;background:#111;color:#fff;border-radius:8px;font-weight:700;text-decoration:none;">▶ 영상 열기</a></div>`;
    else {
      const drive = LIVE.state.driveFolderUrl
        ? `<div style="margin-top:16px;"><button type="button" onclick="window.open('${LIVE.state.driveFolderUrl.replace(/'/g,"\\'")}','_blank','noopener')" style="cursor:pointer;padding:10px 18px;background:#E84855;color:#fff;border:none;border-radius:8px;font-size:13px;font-weight:700;">📁 Google Drive 폴더에서 소재 찾기</button></div>` : '';
      preview = `<div style="padding:28px;background:#f9fafb;border:1.5px dashed #d1d5db;border-radius:12px;text-align:center;color:#6b7280;"><div style="font-size:40px;">${isVideo ? '▶' : '📂'}</div><div style="font-size:13px;font-weight:600;color:#374151;margin-top:8px;">미리보기 링크 없음</div><div style="font-size:11px;font-family:monospace;color:#9ca3af;margin-top:6px;">${escapeHtml(c.파일명 || '-')}</div>${drive}</div>`;
    }
    const w = { meta: c };   // 복제 헬퍼는 creative.meta.* 를 읽음
    const intent = (c.creator_intent || '').trim();
    const intentHtml = intent ? `<div style="font-style:italic;color:#666;margin:8px 0;">${escapeHtml(intent)}</div>` : '';
    el('liveModalBody').innerHTML =
      preview +
      `<h3 style="margin:16px 0 6px;font-size:16px;">${escapeHtml(c.소재명 || '')} <span style="font-size:12px;color:#9ca3af;">(${escapeHtml(c.유형 || '')})</span></h3>` +
      intentHtml + buildInsightBlocksHtml(w) + buildSignalChipsHtml(w);
    el('liveModal').classList.add('active');
  };
```
모달 닫기 배선(`init()` 안):
```javascript
  el('liveModalClose').addEventListener('click', () => el('liveModal').classList.remove('active'));
  el('liveModal').addEventListener('click', e => { if (e.target.id === 'liveModal') el('liveModal').classList.remove('active'); });
```

- [ ] **Step 5: preview 검증 — 그리드 + 모달**

1. `?title=pepp-us` 로드 후 `preview_eval`: `JSON.stringify({cards: document.querySelectorAll('.live-card').length, creatives: LIVE.state.creatives.length})` → 기대: cards === creatives.length (전체 표시)
2. **집행 전 소재 확인**: `preview_eval`로 노출 0 소재 카드도 존재하는지 — `document.querySelectorAll('.live-card').length` 가 전체 소재 수와 동일
3. 카드 클릭 → 모달: `preview_eval`: `(function(){LIVE.openModal(LIVE.state.creatives[0]);var b=document.getElementById('liveModalBody').innerHTML;return JSON.stringify({active:document.getElementById('liveModal').classList.contains('active'),hasInsight:b.includes('인사이트')||b.includes('미리보기 링크 없음'),hasName:b.includes(LIVE.state.creatives[0].소재명||'')});})()` → 기대: active=true, hasName=true
4. tougenanki 로드: 그리드 카드 = 28, 카드 클릭 → 모달에 Drive 버튼(드라이브 폴더 URL 보유) + 분석
5. `preview_snapshot` + `preview_console_logs` error → 없음

- [ ] **Step 6: 커밋**

```bash
cd C:\claude\cloop_dashboard
git add js/live-dashboard.js
git commit -m "feat(live): 전체 소재 그리드 + 분석 모달 + Drive 폴백"
```

---

## Task 5: 통합 검증 + step1 무회귀 + push

**Files:** 없음(검증)

- [ ] **Step 1: 필터 → 그래프 갱신 / 그리드 유지 검증**

`?title=pepp-us` 로드 후 preview:
1. 국가 체크박스 'US' 체크 → `preview_eval`로 차트 라인 데이터 변동 확인 + 그리드 카드 수 **불변**(전체 유지)
   ```js
   (function(){var before=document.querySelectorAll('.live-card').length; var cb=[...document.querySelectorAll('input[data-filter="countries"]')].find(x=>x.value==='US'); cb.click(); var after=document.querySelectorAll('.live-card').length; return JSON.stringify({gridUnchanged: before===after, chartLines: LIVE.chart?LIVE.chart.data.datasets.length:0});})()
   ```
   기대: gridUnchanged=true (그리드는 필터에 숨지 않음)
2. 기간 '최근 7일' 클릭 → 차트 라벨(일자) 줄어듦, 그리드 카드 수 불변

- [ ] **Step 2: step1 무회귀 확인**

`step1_integrated.html?title=pepp-us` 로드 후 분석 실행 → `preview_eval`:
```js
JSON.stringify({rawData: (typeof rawData!=='undefined'?rawData.length:'?'), navHasLive: !!document.querySelector('.cl-nav a[href="live_dashboard.html"]')})
```
기대: rawData=70(기존 동작 무회귀), navHasLive=true(네비 링크만 추가됨). 소재 1개 모달 열어 분석 정상 표시 확인.

- [ ] **Step 3: 전체 콘솔 에러 확인**

live_dashboard.html(pepp·tougenanki) + step1 각각 `preview_console_logs` level=error → 모두 0.

- [ ] **Step 4: push**

```bash
cd C:\claude\cloop_dashboard
git push origin main
```

---

## 검증 체크리스트 (완료 기준)

- [ ] 네비 "라이브 대시보드" 4페이지 + 신규 페이지 표시, active 정상
- [ ] 펩: 통합 그래프(획득 상위 Top N) + 노출/획득/CPI 토글 + 전체 소재 그리드
- [ ] 국가/OS/캠페인 필터 옵션이 campaign_name에서 도출(US/CA/SEA/PH, iOS/미상)
- [ ] 필터 적용 시 **그래프만 변동, 그리드는 전체 소재 유지**(집행 전 포함)
- [ ] 그리드 카드 클릭 → 분석 모달(인사이트·신호칩·미리보기/Drive)
- [ ] 도원암귀: 그래프 "데이터 없음" + 그리드 28개 + 모달 동작(Drive 버튼)
- [ ] step1 무회귀(네비 링크만 추가) · 콘솔 에러 0
