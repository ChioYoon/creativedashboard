# 캠페인 타입 필터 (Phase 2) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 캠페인명 캐노니컬 필드(ua_type/country/os/media/product)를 파이프라인 출력 JSON에 맵으로 저장하고, step1·라이브 대시보드에 캠페인 유형별 필터를 추가해 동질 비교를 가능하게 한다.

**Architecture:** A안 — 파이프라인이 `campaign_canonical` top-level 맵(`campaign_name→필드`)을 산출하고, 대시보드는 이 맵을 조회해 캐노니컬 필터 선택값을 "campaign_name 집합"으로 변환, step1의 기존 KPI-swap·라이브의 기존 Set 필터 엔진에 교집합으로 주입한다. 라이브의 JS country/os 파서는 파이프라인 캐노니컬로 일원화(조회 단위 폴백 유지).

**Tech Stack:** Python 3 / pydantic (파이프라인), 정적 HTML + 바닐라 JS (대시보드), pytest (파이프라인 테스트), preview 도구 (대시보드 검증).

## Global Constraints

- 캐노니컬 추출 우선순위: `ua_type`=기존 `campaign_ua_type`(token, NU-Pre 우선) 재사용 / `country`·`os`=신규 token-scan 헬퍼(라이브 JS 로직 이식) / `media`·`product`=positional `parse_campaign_canonical` best-effort.
- 추출 실패값은 빈 문자열 `""` (대시보드가 `미상` 버킷 처리). 어떤 행도 누락 이유로 제외하지 않는다.
- 필터 동작: 차원 간 **AND**, 차원 내 **OR**. 빈 선택 = 필터 미적용(전체).
- 하위호환: `campaign_canonical` 부재(구 JSON·KPI 미연동 타이틀) 시 — 라이브는 country/os를 **조회 단위로** 기존 JS 파서 폴백하고 ua_type/media/product 필터 미표시, step1은 캐노니컬 필터 섹션 숨김. 무크래시.
- 기존 집계 로직(`aggGadsWindow`/`aggMmpWindow`/KPI-swap/`calculateScores`)의 산식은 변경하지 않는다 — 캐노니컬은 campaign_name 집합으로만 주입.
- `.env`/`.secrets/google_ads.yaml` 절대 읽기·출력·커밋 금지. 본 작업은 API 호출 0(순수 파싱).
- 한글 라벨맵·date 범위 필터·agency/executor 필터·세그먼트 비교 뷰는 범위 밖.

---

### Task 1: 캐노니컬 추출 헬퍼 + 맵 빌더 (`campaign_canonical.py`)

**Files:**
- Modify: `pipeline/campaign_canonical.py` (기존 `parse_campaign_canonical`·`campaign_ua_type` 유지, 헬퍼 3개 추가)
- Test: `tests/test_campaign_canonical.py` (기존 테스트 유지, 신규 테스트 추가)

**Interfaces:**
- Consumes: 기존 `parse_campaign_canonical(name) -> dict`, `campaign_ua_type(name) -> str`.
- Produces:
  - `campaign_country(name: str) -> str` — `XX-XX` 세그먼트의 `-` 앞부분, 없으면 `""`.
  - `campaign_os(name: str) -> str` — ios/aos/android/web 토큰 → `iOS`/`Android`/`Web`, 없으면 `""`.
  - `build_campaign_canonical(campaign_names) -> dict` — `{campaign_name: {"ua_type","country","os","media","product"}}`. 중복 제거, 빈/None 입력 제외, 빈 입력 → `{}`.

- [ ] **Step 1: Write the failing tests**

`tests/test_campaign_canonical.py` 상단 import 교체 + 파일 끝에 테스트 추가:

```python
from pipeline.campaign_canonical import (
    parse_campaign_canonical, campaign_ua_type,
    campaign_country, campaign_os, build_campaign_canonical,
)


def test_campaign_country():
    assert campaign_country("HQ_HQ_PH_US-EN_GA_NU_AD_ACA-PU_260429") == "US"
    assert campaign_country("Maximizer_HQ_GD_KR-KR_GA_NU-Pre_AD_ACp_241218") == "KR"
    assert campaign_country("no_country_here") == ""
    assert campaign_country("") == ""


def test_campaign_os():
    assert campaign_os("HQ_HQ_PH_CA-EN_FB_NU_iOS_INSTALL_260122") == "iOS"
    assert campaign_os("HQ_HQ_PH_US-EN_GA_NU_AOS_ACA_260101") == "Android"
    assert campaign_os("x_android_y") == "Android"
    assert campaign_os("a_web_b") == "Web"
    assert campaign_os("no_os_token") == ""
    assert campaign_os("") == ""


def test_build_campaign_canonical_basic():
    names = ["HQ_HQ_PH_US-EN_GA_NU-Pre_iOS_ACp_260429"]
    m = build_campaign_canonical(names)
    assert set(m.keys()) == set(names)
    e = m[names[0]]
    assert e["ua_type"] == "NU-Pre" and e["country"] == "US" and e["os"] == "iOS"
    assert e["media"] == "GA" and e["product"] == "ACp"
    assert set(e.keys()) == {"ua_type", "country", "os", "media", "product"}


def test_build_campaign_canonical_dedup_and_missing():
    m = build_campaign_canonical(["A_B_C", "A_B_C", "", None])
    assert list(m.keys()) == ["A_B_C"]            # 중복 제거 + 빈/None 제외
    assert m["A_B_C"] == {"ua_type": "", "country": "", "os": "", "media": "", "product": ""}


def test_build_campaign_canonical_empty():
    assert build_campaign_canonical([]) == {}
    assert build_campaign_canonical(None) == {}
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd /c/claude/cloop_dashboard && PYTHONUTF8=1 PYTHONPATH=/c/claude/cloop_dashboard python -m pytest tests/test_campaign_canonical.py -v`
Expected: FAIL — `ImportError: cannot import name 'campaign_country'` (및 `campaign_os`, `build_campaign_canonical`).

- [ ] **Step 3: Implement the helpers**

`pipeline/campaign_canonical.py` — 기존 `campaign_ua_type` 함수 뒤(파일 끝)에 추가:

```python
_COUNTRY_RE = re.compile(r"^[A-Z]{2,4}-[A-Z]{2}$")   # US-EN, CA-FR, KR-KR …
_OS_MAP = {"ios": "iOS", "aos": "Android", "android": "Android", "web": "Web"}


def campaign_country(name: str) -> str:
    """캠페인명에서 country 추출 — XX-XX 세그먼트 스캔 후 '-' 앞부분. 없으면 ''. (라이브 JS parseCountry 이식)"""
    if not name:
        return ""
    for seg in name.split("_"):
        if _COUNTRY_RE.match(seg):
            return seg.split("-")[0]
    return ""


def campaign_os(name: str) -> str:
    """캠페인명에서 os 추출 — 토큰 소문자 매칭 → iOS/Android/Web. 없으면 ''. (라이브 JS parseOS 이식)"""
    if not name:
        return ""
    for seg in name.split("_"):
        v = _OS_MAP.get(seg.lower())
        if v:
            return v
    return ""


def build_campaign_canonical(campaign_names) -> dict:
    """고유 campaign_name → {ua_type, country, os, media, product} 맵.

    중복 제거·빈/None 제외. 추출 실패 필드는 '' (대시보드가 '미상' 버킷 처리). 빈 입력 → {}.
    """
    out: dict = {}
    for cn in {c for c in (campaign_names or []) if c}:
        pos = parse_campaign_canonical(cn)
        out[cn] = {
            "ua_type": campaign_ua_type(cn),            # token (견고)
            "country": campaign_country(cn),            # XX-XX 스캔 (라이브 일치)
            "os": campaign_os(cn),                      # 토큰 스캔 (라이브 일치)
            "media": pos["media"] or "",                # positional best-effort
            "product": pos["product"] or "",
        }
    return out
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd /c/claude/cloop_dashboard && PYTHONUTF8=1 PYTHONPATH=/c/claude/cloop_dashboard python -m pytest tests/test_campaign_canonical.py -v`
Expected: PASS — 기존 테스트 + 신규 5개 전부 통과.

- [ ] **Step 5: Commit**

```bash
git add pipeline/campaign_canonical.py tests/test_campaign_canonical.py
git commit -m "feat(canonical): campaign_country·campaign_os·build_campaign_canonical 추가"
```

---

### Task 2: 출력 스키마 + 파이프라인 배선 (`schemas.py`, `main.py`)

**Files:**
- Modify: `pipeline/schemas.py:574` (`CreativeDataset`에 필드 추가)
- Modify: `pipeline/main.py:42-47` (import), `pipeline/main.py:1011-1028` (배선), 모듈 레벨 헬퍼 추가
- Test: `tests/test_campaign_canonical.py` (스키마·수집 헬퍼 테스트 추가)

**Interfaces:**
- Consumes: `build_campaign_canonical` (Task 1).
- Produces:
  - `CreativeDataset.campaign_canonical: dict` (기본 `{}`) — 산출 JSON top-level 키.
  - `pipeline.main._collect_campaign_names(records) -> set` — records 의 kpi_daily + mmp_daily 에서 비어있지 않은 campaign_name 수집.

- [ ] **Step 1: Write the failing tests**

`tests/test_campaign_canonical.py` 끝에 추가:

```python
def test_dataset_has_campaign_canonical_default():
    from pipeline.schemas import CreativeDataset
    ds = CreativeDataset(title_id="t", generated_at="2026-01-01T00:00:00+09:00")
    payload = ds.model_dump(by_alias=True)
    assert payload["campaign_canonical"] == {}


def test_dataset_serializes_campaign_canonical():
    from pipeline.schemas import CreativeDataset
    m = {"A_B_C_US-EN_GA_NU_AD_ACp": {"ua_type": "NU", "country": "US", "os": "", "media": "GA", "product": "ACp"}}
    ds = CreativeDataset(title_id="t", generated_at="2026-01-01T00:00:00+09:00", campaign_canonical=m)
    assert ds.model_dump(by_alias=True)["campaign_canonical"] == m


def test_collect_campaign_names():
    from pipeline.main import _collect_campaign_names

    class _Row:  # duck-typed (pydantic 모델 불필요)
        def __init__(self, kpi, mmp):
            self.kpi_daily = kpi
            self.mmp_daily = mmp

    class _Daily:
        def __init__(self, cn):
            self.campaign_name = cn

    recs = [
        _Row([_Daily("A_B_C"), _Daily("")], [_Daily("D_E_F")]),
        _Row([_Daily("A_B_C")], []),       # 중복
        _Row([], None),                    # mmp_daily None
    ]
    assert _collect_campaign_names(recs) == {"A_B_C", "D_E_F"}
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd /c/claude/cloop_dashboard && PYTHONUTF8=1 PYTHONPATH=/c/claude/cloop_dashboard python -m pytest tests/test_campaign_canonical.py -k "dataset or collect" -v`
Expected: FAIL — `KeyError: 'campaign_canonical'` (스키마 필드 부재) 및 `ImportError`/`AttributeError: _collect_campaign_names`.

- [ ] **Step 3a: Add the schema field**

`pipeline/schemas.py` — `CreativeDataset`의 `metrics` 필드(line 574) 바로 뒤에 추가:

```python
    # 메트릭 (대시보드 신뢰도 배지 등에서 활용 가능)
    metrics: dict = Field(default_factory=dict)

    # Phase 2: 캠페인명 캐노니컬 필드 맵 (campaign_name → {ua_type,country,os,media,product})
    campaign_canonical: dict = Field(default_factory=dict, description="캠페인 유형 필터용")
```

- [ ] **Step 3b: Add the import and helper in main.py**

`pipeline/main.py` — import 블록(line 42-47)에 추가:

```python
from .cache import TagCache, file_sha256
from .campaign_canonical import build_campaign_canonical
from .mmp_metrics import aggregate_rows_total, compute_mmp_quality
```

`pipeline/main.py` — `_load_game_context` 함수(line 59) 앞에 모듈 레벨 헬퍼 추가:

```python
def _collect_campaign_names(records) -> set:
    """records 의 kpi_daily + mmp_daily 행에서 비어있지 않은 campaign_name 수집."""
    names = set()
    for r in records:
        for k in (getattr(r, "kpi_daily", None) or []):
            cn = getattr(k, "campaign_name", "")
            if cn:
                names.add(cn)
        for m in (getattr(r, "mmp_daily", None) or []):
            cn = getattr(m, "campaign_name", "")
            if cn:
                names.add(cn)
    return names
```

- [ ] **Step 3c: Wire into CreativeDataset construction**

`pipeline/main.py` — `CreativeDataset(...)` 호출(line 1011-1028)에서 `metrics={...}` 블록 뒤에 인자 추가:

```python
        metrics={
            "scanned_folders": len(candidates),
            "tagged_records": len(records),
            "cache_hits": hits,
            "cache_misses": misses,
            "failures": failures,
            "duration_sec": round(duration, 1),
            "prompt_version": pversion,
            "fallback_used": metrics["fallback_used"],
            "carried_forward": carried_forward,
            "score_summary": score_summary,  # Stage 6: 기본 가중치 점수 요약
        },
        campaign_canonical=build_campaign_canonical(_collect_campaign_names(records)),
    )
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd /c/claude/cloop_dashboard && PYTHONUTF8=1 PYTHONPATH=/c/claude/cloop_dashboard python -m pytest tests/test_campaign_canonical.py -v`
Expected: PASS — 신규 8개 + 기존 테스트 전부.

전체 회귀: `PYTHONUTF8=1 PYTHONPATH=/c/claude/cloop_dashboard python -m pytest -q`
Expected: 기존 78 + 신규 통과, 실패 0.

- [ ] **Step 5: Commit**

```bash
git add pipeline/schemas.py pipeline/main.py tests/test_campaign_canonical.py
git commit -m "feat(pipeline): CreativeDataset.campaign_canonical 맵 산출 배선"
```

---

### Task 3: 라이브 대시보드 캐노니컬 일원화 + 필터 (`live-dashboard.js`)

**Files:**
- Modify: `js/live-dashboard.js` (LIVE.canon 로드, allDailies 파서 일원화, applyFilters 게이트 3종, buildFilters 그룹 3종)

**Interfaces:**
- Consumes: 산출 JSON top-level `campaign_canonical` (Task 2) — `data.campaign_canonical`.
- Produces: 라이브 그리드·트렌드에 ua_type/media/product 필터 + country/os 캐노니컬 일원화. 맵 부재 시 무회귀 폴백.

**참고(불변):** 체크박스 change 핸들러(`js/live-dashboard.js:91-95`)는 `LIVE.state.filters[cb.dataset.filter]` 를 동적으로 읽으므로, `data-filter="ua_types|medias|products"` 체크박스는 추가 배선 없이 동작한다.

- [ ] **Step 1: LIVE.canon 초기화 + 로드**

`js/live-dashboard.js` — LIVE 객체 리터럴(line 4-9)에 `canon` 추가:

```javascript
  const LIVE = {
    state: { titleId: '', creatives: [], driveFolderUrl: '', hasKpi: false, manifest: [] },
    canon: {},
    metric: 'impr',
    topN: 7,
    chart: null,
  };
```

`LIVE.loadTitle`(line 356) — `const creatives = data.creatives || [];` 바로 뒤에 추가:

```javascript
    const creatives = data.creatives || [];
    LIVE.canon = (data.campaign_canonical && typeof data.campaign_canonical === 'object') ? data.campaign_canonical : {};
```

- [ ] **Step 2: allDailies 파서 일원화**

`js/live-dashboard.js` — `LIVE.allDailies`(line 33-48) 전체 교체:

```javascript
  LIVE.allDailies = function () {
    const out = [];
    const canon = LIVE.canon || {};
    // country/os: 캐노니컬 값 있으면 사용, 비었거나 항목 없으면 조회 단위로 JS 파서 폴백
    const cf = (cn, field, jsFn) => { const v = canon[cn] && canon[cn][field]; return v ? v : jsFn(cn); };
    // ua_type/media/product: 캐노니컬 전용 (맵 없으면 '미상' — 필터도 미표시)
    const cv = (cn, field) => (canon[cn] && canon[cn][field]) || '미상';
    for (const c of LIVE.state.creatives) {
      for (const k of (c.kpi_daily || [])) {
        out.push({ creative: c, date: k.date, campaign_name: k.campaign_name,
          country: cf(k.campaign_name, 'country', LIVE.parseCountry), os: cf(k.campaign_name, 'os', LIVE.parseOS),
          ua_type: cv(k.campaign_name, 'ua_type'), media: cv(k.campaign_name, 'media'), product: cv(k.campaign_name, 'product'),
          src: 'GA', impr: +k.impressions || 0, acq: +k.conversions || 0, cost: +k.cost || 0 });
      }
      for (const m of (c.mmp_daily || [])) {
        out.push({ creative: c, date: m.date, campaign_name: m.campaign_name,
          country: cf(m.campaign_name, 'country', LIVE.parseCountry), os: cf(m.campaign_name, 'os', LIVE.parseOS),
          ua_type: cv(m.campaign_name, 'ua_type'), media: cv(m.campaign_name, 'media'), product: cv(m.campaign_name, 'product'),
          src: 'MMP', impr: +m.impressions || 0, acq: +m.installs || 0, cost: +m.cost || 0 });
      }
    }
    return out;
  };
```

- [ ] **Step 3: applyFilters 게이트 3종 추가**

`js/live-dashboard.js` — `LIVE.applyFilters`(line 50-60) 전체 교체:

```javascript
  LIVE.applyFilters = function () {
    const f = LIVE.state.filters || { start:'', end:'', countries:new Set(), oses:new Set(), campaigns:new Set(), ua_types:new Set(), medias:new Set(), products:new Set() };
    return LIVE.allDailies().filter(d => {
      if (f.start && d.date < f.start) return false;
      if (f.end && d.date > f.end) return false;
      if (f.countries.size && !f.countries.has(d.country)) return false;
      if (f.oses.size && !f.oses.has(d.os)) return false;
      if (f.ua_types && f.ua_types.size && !f.ua_types.has(d.ua_type)) return false;
      if (f.medias && f.medias.size && !f.medias.has(d.media)) return false;
      if (f.products && f.products.size && !f.products.has(d.product)) return false;
      if (f.campaigns.size && !f.campaigns.has(d.campaign_name)) return false;
      return true;
    });
  };
```

- [ ] **Step 4: buildFilters — 상태 init + 그룹 3종 (맵 있을 때만)**

`js/live-dashboard.js` — `LIVE.buildFilters` 내부 distinct 계산부(line 67-73)를 교체:

```javascript
    const countries = [...new Set(ds.map(d => d.country))].sort();
    const oses = [...new Set(ds.map(d => d.os))].sort();
    const camps = [...new Set(ds.map(d => d.campaign_name).filter(Boolean))].sort();
    const hasCanon = LIVE.canon && Object.keys(LIVE.canon).length > 0;
    const uaTypes  = hasCanon ? [...new Set(ds.map(d => d.ua_type))].sort() : [];
    const medias   = hasCanon ? [...new Set(ds.map(d => d.media))].sort()   : [];
    const products = hasCanon ? [...new Set(ds.map(d => d.product))].sort() : [];
    const dates = ds.map(d => d.date).filter(Boolean).sort();
    LIVE.state._maxDate = dates[dates.length - 1] || '';
    LIVE.state._minDate = dates[0] || '';
    LIVE.state.filters = { start:'', end:'', countries:new Set(), oses:new Set(), campaigns:new Set(), ua_types:new Set(), medias:new Set(), products:new Set() };
```

`js/live-dashboard.js` — `host.innerHTML = ...` 의 OS 그룹(line 84) 뒤, 캠페인 그룹 앞에 캐노니컬 3종 삽입:

```javascript
      `<div class="live-filter-group"><label>OS</label><div>${checks('oses', oses)}</div></div>` +
      (hasCanon ?
        `<div class="live-filter-group"><label>유형</label><div>${checks('ua_types', uaTypes)}</div></div>` +
        `<div class="live-filter-group"><label>매체</label><div>${checks('medias', medias)}</div></div>` +
        `<div class="live-filter-group"><label>상품</label><div>${checks('products', products)}</div></div>` : '') +
      `<div class="live-filter-group" style="max-width:360px;"><label>캠페인` +
```

- [ ] **Step 5: 검증용 캐노니컬 맵 생성 (preview 주입용, 파일 미생성)**

Run (맵 JSON 한 줄 출력 — preview_eval 로 주입):
```bash
cd /c/claude/cloop_dashboard && PYTHONUTF8=1 PYTHONPATH=/c/claude/cloop_dashboard python -c "
import json
from pipeline.campaign_canonical import build_campaign_canonical
d=json.load(open('public/data/pepp-us.json',encoding='utf-8'))
names=set()
for c in d['creatives']:
    for k in (c.get('kpi_daily') or []):
        if k.get('campaign_name'): names.add(k['campaign_name'])
    for m in (c.get('mmp_daily') or []):
        if m.get('campaign_name'): names.add(m['campaign_name'])
print(json.dumps(build_campaign_canonical(names),ensure_ascii=False))
"
```
Expected: `{"...campaign...": {"ua_type": "...", "country": "...", ...}, ...}` 형태의 비어있지 않은 JSON. 이 출력을 복사해 Step 6에서 사용.

- [ ] **Step 6: Preview 검증 — 맵 부재(폴백) + 맵 존재(필터)**

1. `preview_start` (정적 서버), `live_dashboard.html?title=pepp-us` 로드.
2. **맵 부재 경로** (현재 pepp-us.json 에는 campaign_canonical 없음): `preview_snapshot`/`preview_console_logs` 로 (a) 콘솔 에러 없음, (b) 국가·OS 필터는 표시(JS 폴백), (c) 유형·매체·상품 필터 **미표시** 확인. `preview_screenshot` 증빙.
3. **맵 존재 경로**: `preview_eval` 로 주입 —
   ```js
   LIVE.canon = /* Step 5 출력 JSON 붙여넣기 */;
   LIVE.buildFilters(); LIVE.render();
   ```
   (a) 유형·매체·상품 필터가 채워짐 확인(`preview_snapshot`), (b) 유형 체크박스 하나 선택 후 `preview_click` → 트렌드/그리드 획득 배지가 해당 유형만 반영되도록 변하는지 확인, (c) 값이 빈 캠페인이 `미상`으로 묶이는지 확인. `preview_screenshot` 증빙.

- [ ] **Step 7: Commit**

```bash
git add js/live-dashboard.js
git commit -m "feat(live): 캐노니컬 country/os 일원화 + ua_type/media/product 필터 (폴백 유지)"
```

---

### Task 4: step1 캐노니컬 필터 (`data-source.js`, `step1_integrated.html`)

**Files:**
- Modify: `js/data-source.js:139-145` (`normalizeFromJson._meta` 에 campaign_canonical 전달)
- Modify: `step1_integrated.html` — 필터 UI(line 1540 앞), 전역(line 3423), 로드 배선(line 3537·3775), 함수 추가, `calculateScores` 교집합(line 4189·4206-4207)

**Interfaces:**
- Consumes: 산출 JSON top-level `campaign_canonical` (Task 2). step1 은 `DataSource.normalizeFromJson` 경유 → `_meta.campaign_canonical`.
- Produces: step1 사이드바 "캠페인 유형" 필터(5차원) → `calculateScores` 에서 campaign_name 집합으로 변환해 기존 캠페인 KPI-swap 흐름에 교집합 주입.

**참고(불변):** `calculateScores`(line 4174)가 명시적 "분석 실행" 버튼으로 호출되는 메인 렌더 트리거. 캐노니컬 필터는 상태만 갱신하고 calculateScores 시작부에서 교집합을 적용한다(캠페인 필터와 동일 패턴 — 즉시 렌더 X). 캠페인 값은 GA=raw campaign_name, MMP=`[MMP] `+(campaign_name‖channel) 접두어. 캐노니컬 맵 키는 raw campaign_name 이므로 `[MMP] ` 접두어(6자)를 strip 후 조회.

- [ ] **Step 1: data-source.js — _meta 전달**

`js/data-source.js` — `normalizeFromJson` 의 `_meta` 객체(line 139-145)에 키 추가:

```javascript
      _meta: {
        source: 'json',
        title_id: data.title_id || '',
        generated_at: data.generated_at || '',
        schema_version: data.schema_version || DataSourceMeta.SCHEMA_VERSION,
        creative_count: rows.length,
        campaign_canonical: (data.campaign_canonical && typeof data.campaign_canonical === 'object') ? data.campaign_canonical : {},
      },
```

- [ ] **Step 2: step1 — 필터 UI 섹션 추가**

`step1_integrated.html` — "캠페인" 라벨(line 1540) **바로 앞**에 삽입:

```html
            <div id="canonicalFilterSection" style="display:none;">
              <span class="sb-label">캠페인 유형</span>
              <div id="canonicalFilterGroups"></div>
            </div>

            <span class="sb-label">캠페인</span>
```

- [ ] **Step 3: step1 — 전역 변수**

`step1_integrated.html` — `let currentCampaign = '';`(line 3423) 뒤에 추가:

```javascript
    let currentCampaign = '';
    let currentCanonical = { ua_type: new Set(), country: new Set(), os: new Set(), media: new Set(), product: new Set() };
    window.__campaignCanonical = {};
```

- [ ] **Step 4: step1 — 캐노니컬 필터 함수 추가**

`step1_integrated.html` — `filterCampaignList()` 함수(line 4090-4103) 전체 교체 + 신규 함수 추가:

```javascript
    // 캠페인 검색 + 캐노니컬 narrowing 통합 (단일 가시성 패스)
    function filterCampaignList() {
      refreshCampaignVisibility();
    }

    const CANON_DIMS = [
      { key: 'ua_type', label: '캠페인 목적' },
      { key: 'country', label: '국가' },
      { key: 'os', label: 'OS' },
      { key: 'media', label: '매체' },
      { key: 'product', label: '상품' },
    ];

    // 산출 JSON 의 campaign_canonical 맵 → 5차원 체크박스 그룹 렌더 (맵 없으면 섹션 숨김)
    function populateCanonicalFilter() {
      const map = window.__campaignCanonical || {};
      const section = document.getElementById('canonicalFilterSection');
      const host = document.getElementById('canonicalFilterGroups');
      CANON_DIMS.forEach(d => currentCanonical[d.key] = new Set());   // 상태 초기화
      if (!Object.keys(map).length) { if (section) section.style.display = 'none'; if (host) host.innerHTML = ''; return; }
      const values = {};
      CANON_DIMS.forEach(d => values[d.key] = new Set());
      Object.values(map).forEach(f => CANON_DIMS.forEach(d => values[d.key].add((f && f[d.key]) || '미상')));
      host.innerHTML = CANON_DIMS.map(d => {
        const opts = [...values[d.key]].sort().map(v =>
          `<label style="display:inline-flex;align-items:center;gap:3px;font-size:11px;margin:0 6px 4px 0;cursor:pointer;">` +
          `<input type="checkbox" data-canon="${d.key}" value="${v}" onchange="onCanonicalChange()" style="margin:0;">${v}</label>`).join('');
        return `<div style="margin-bottom:6px;"><div style="font-size:11px;font-weight:700;color:#6b7280;margin-bottom:2px;">${d.label}</div><div>${opts}</div></div>`;
      }).join('');
      if (section) section.style.display = '';
    }

    // 캐노니컬 체크박스 변경 → 상태 갱신 + 캠페인 목록 좁혀 표시 (렌더는 분석 실행 버튼)
    function onCanonicalChange() {
      CANON_DIMS.forEach(d => currentCanonical[d.key] = new Set());
      document.querySelectorAll('#canonicalFilterGroups input[type="checkbox"]:checked').forEach(cb => {
        currentCanonical[cb.dataset.canon].add(cb.value);
      });
      refreshCampaignVisibility();
    }

    // 선택된 캐노니컬 값 → 충족 campaign_name 집합 (차원간 AND, 차원내 OR). 활성 차원 없으면 null.
    function resolveCanonicalCampaigns() {
      const map = window.__campaignCanonical || {};
      const dims = ['ua_type', 'country', 'os', 'media', 'product'];
      const active = dims.filter(d => currentCanonical[d] && currentCanonical[d].size);
      if (!active.length) return null;
      const allowed = new Set();
      for (const cn in map) {
        const f = map[cn] || {};
        if (active.every(d => currentCanonical[d].has((f[d] || '미상')))) allowed.add(cn);
      }
      return allowed;
    }

    // 캠페인 체크박스 가시성 = 검색 텍스트 AND 캐노니컬 허용집합 (둘 다 충족 시만 표시)
    function refreshCampaignVisibility() {
      const searchInput = document.getElementById('campaignSearchInput');
      const searchText = (searchInput ? searchInput.value : '').toLowerCase();
      const allowed = resolveCanonicalCampaigns();   // Set<raw> 또는 null
      const container = document.getElementById('campaignCheckboxGroup');
      if (!container) return;
      container.querySelectorAll('.checkbox-item:not(:first-child)').forEach(item => {
        const cb = item.querySelector('input[type="checkbox"]');
        const val = cb ? cb.value : '';
        const raw = val.startsWith('[MMP] ') ? val.slice(6) : val;
        const matchSearch = (item.dataset.campaign || '').includes(searchText);
        const matchCanon = !allowed || allowed.has(raw);
        item.style.display = (matchSearch && matchCanon) ? 'flex' : 'none';
      });
    }
```

- [ ] **Step 5: step1 — 로드 배선 (JSON·CSV 양쪽)**

`step1_integrated.html` — JSON 로드부(line 3536-3538)를 교체:

```javascript
        // 캠페인·날짜 갱신 (기존 흐름 재사용)
        window.__campaignCanonical = (normalized._meta && normalized._meta.campaign_canonical) || {};
        const campaigns = extractCampaigns(rawData);
        populateCampaignFilter(campaigns);
        populateCanonicalFilter();
```

`step1_integrated.html` — CSV 로드부(line 3774-3776)를 교체:

```javascript
        window.__campaignCanonical = {};   // CSV 는 캐노니컬 맵 없음
        const campaigns = extractCampaigns(rawData);
        populateCampaignFilter(campaigns);
        populateCanonicalFilter();
```

- [ ] **Step 6: step1 — calculateScores 교집합 주입**

`step1_integrated.html` — `let filterSummary = [];`(line 4192) 뒤, `let activeMmpCampaigns`(line 4195) 앞에 삽입:

```javascript
          // 필터 조건 수집 (v1.7: 요약 표시용)
          let filterSummary = [];

          // ── 캐노니컬(캠페인 유형) 필터 → currentCampaign 과 교집합 ──
          let effectiveCampaign = currentCampaign;
          const canonAllowed = resolveCanonicalCampaigns();   // Set<raw> 또는 null
          if (canonAllowed) {
            const base = Array.isArray(currentCampaign)
              ? currentCampaign
              : Array.from(document.querySelectorAll('#campaignCheckboxGroup input[type="checkbox"]:not(#campaign_all)')).map(cb => cb.value);
            effectiveCampaign = base.filter(v => canonAllowed.has(v.startsWith('[MMP] ') ? v.slice(6) : v));
            filterSummary.push(`캠페인 유형 필터 → ${effectiveCampaign.length}개 캠페인`);
            if (!effectiveCampaign.length) effectiveCampaign = [' __canon_no_match__'];  // 매칭 0 → 결과 0
          }
```

`step1_integrated.html` — 캠페인 필터 블록 진입부(line 4206-4207)를 `effectiveCampaign` 사용으로 교체:

```javascript
          if (effectiveCampaign) {
            const selectedCampaigns = Array.isArray(effectiveCampaign) ? effectiveCampaign : [effectiveCampaign];
```

(이하 블록의 `gadsCampaigns`/`mmpChannels` 분리·KPI-swap 로직은 변경 없음. 빈 매칭 시 sentinel 캠페인은 모든 행의 kpi_daily 와 불일치 → 결과 0.)

- [ ] **Step 7: Preview 검증**

1. `preview_start`, `step1_integrated.html?title=pepp-us` 로드.
2. **맵 부재 경로**(현 pepp-us.json): "캠페인 유형" 섹션 **미표시**(`canonicalFilterSection` display:none) + 콘솔 에러 없음 + 기존 캠페인 필터·분석 실행 정상. `preview_screenshot` 증빙.
3. **맵 존재 경로**: `preview_eval` 로 주입 (Task 3 Step 5 출력 JSON 사용) —
   ```js
   window.__campaignCanonical = /* Step 5 출력 JSON */;
   populateCanonicalFilter();
   ```
   (a) "캠페인 유형" 5차원 필터가 채워짐(`preview_snapshot`), (b) 한 차원 값 체크 → 하단 캠페인 목록이 해당 집합으로 좁혀짐(`preview_snapshot`), (c) "분석 실행" 클릭(`preview_click`) → 표/점수가 해당 캠페인 KPI 합으로 재계산됨, (d) 매칭 없는 조합 선택 → 결과 0행. `preview_screenshot` 증빙.

- [ ] **Step 8: Commit**

```bash
git add js/data-source.js step1_integrated.html
git commit -m "feat(step1): 캠페인 유형 캐노니컬 필터 + KPI-swap 교집합 주입"
```

---

## Self-Review

**1. Spec coverage** (스펙 섹션 → 태스크 매핑):
- C1 캐노니컬 헬퍼·맵 빌더 → Task 1 ✅
- C2 schemas 필드 → Task 2 ✅
- C3 main.py 배선 → Task 2 ✅
- C4 step1 필터+resolveCanonicalCampaigns+KPI-swap 주입 → Task 4 ✅
- C5 라이브 일원화+필터 3종 → Task 3 ✅
- 필드별 추출 전략(ua_type token/country·os scan/media·product positional) → Task 1 ✅
- 하위호환(맵 부재 폴백·섹션 숨김) → Task 3 Step 6·Task 4 Step 7 검증 ✅
- 필터 동작(차원간 AND·차원내 OR·미상 버킷) → Task 3 applyFilters·Task 4 resolveCanonicalCampaigns ✅
- 테스트(pytest 헬퍼·맵빌더 / 대시보드 preview) → Task 1·2 pytest, Task 3·4 preview ✅

**2. Placeholder scan:** 모든 코드 스텝에 실제 코드·정확한 명령·기대 출력 포함. TBD/TODO 없음.

**3. Type consistency:** `build_campaign_canonical`(Task 1) 반환 키 `{ua_type,country,os,media,product}` = Task 3 `cv`/`cf` 조회 키 = Task 4 `CANON_DIMS` 키 = `resolveCanonicalCampaigns` 조회 키. `_collect_campaign_names`(Task 2 produces) = Task 2 main 배선 consumes. `data.campaign_canonical`(Task 2 산출) = Task 3 `LIVE.canon` 로드 = Task 4 `normalizeFromJson._meta.campaign_canonical` → `window.__campaignCanonical` 로드. 일관 ✅.

## Execution Handoff

**1. Subagent-Driven (recommended)** — 태스크별 fresh subagent + 리뷰. Task 1·2 는 transcription(저가 모델), Task 3·4 는 통합(표준 모델).
**2. Inline Execution** — executing-plans 로 본 세션 직접 실행.
