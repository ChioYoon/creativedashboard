# ⑧ 성과별 강·약점 태그 분석 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 전체 성과 요약에 최우수/하위 소재의 강·약점 태그 2카드를 추가하되, 점수·집계·기존 패널은 바꾸지 않는다.

**Architecture:** `step1_integrated.html` 인라인. 신규 `buildPerfTagAnalysis(creatives)` + `renderCrossPerformanceCards` 연결 1줄. 표시 전용.

**Tech Stack:** 바닐라 JS. preview 서버 + `preview_eval`(캐시 우회 `?_=<ts>`, 함수 단위 단언).

## Global Constraints

- **점수·등급·집계·기존 'AI 태그' 패널·성과 랭킹 무변경.** 신규 표시 블록만 append.
- 그룹 = TotalScore(Google Ads) 상·하위 각 `k = min(3, floor(n/2))`. 유효 소재 = `_ads.imp>0` AND (strengths 또는 weaknesses 보유). n<4 또는 양쪽 집계 빈 경우 빈 문자열(숨김).
- 카드: 🏆 최우수 공통 강점(top 그룹 strengths Top5) / ⚠️ 하위 공통 약점(bottom 그룹 weaknesses Top5). 지분% = cnt/k.
- 기존 CSS 재사용: `summary-block`·`signal-perf-block`·`signal-perf-card`·`h4`·`ul`·`li`.

---

### Task 1: buildPerfTagAnalysis + 연결

**Files:** Modify `step1_integrated.html` — 신규 함수(`buildPerfRankingBlocks` 6023 뒤, `renderCrossPerformanceCards` 6025 앞), 연결(6072).

- [ ] **Step 1: buildPerfTagAnalysis 함수 추가**

`function renderCrossPerformanceCards(crossData, creatives) {`(6025) **앞**에 추가:
```js
    // ⑧ 성과별 강·약점 태그 — 최우수/하위 소재 그룹의 strengths/weaknesses 태그 집계 (표시 전용)
    function buildPerfTagAnalysis(creatives) {
      const valid = (creatives || []).filter(c =>
        c && c._ads && c._ads.imp > 0 && typeof c.TotalScore === 'number' &&
        ((Array.isArray(c.meta?.strengths) && c.meta.strengths.length) ||
         (Array.isArray(c.meta?.weaknesses) && c.meta.weaknesses.length))
      );
      const n = valid.length;
      if (n < 4) return '';
      const sorted = valid.slice().sort((a, b) => b.TotalScore - a.TotalScore);
      const k = Math.min(3, Math.floor(n / 2));
      const topG = sorted.slice(0, k);
      const botG = sorted.slice(n - k);
      const tally = (group, field) => {
        const m = new Map();
        group.forEach(c => {
          const list = (c.meta && c.meta[field]) || [];
          if (Array.isArray(list)) list.forEach(v => m.set(v, (m.get(v) || 0) + 1));
        });
        return [...m.entries()].sort((a, b) => b[1] - a[1]);
      };
      const topStr = tally(topG, 'strengths');
      const botWk  = tally(botG, 'weaknesses');
      if (!topStr.length && !botWk.length) return '';
      const li = (arr, denom) => arr.slice(0, 5).map(([label, cnt]) =>
        `<li><span>${escapeHtml(label)}</span><span style="color:#6b7280;font-size:12px;white-space:nowrap;">${cnt}건 (${Math.round((cnt / denom) * 100)}%)</span></li>`
      ).join('') || '<li style="color:#9ca3af;">(공통 태그 없음)</li>';
      return `
        <div class="summary-block">
          <div class="summary-block-title">성과별 강·약점 태그 <span style="font-weight:400;font-size:12px;color:var(--text-secondary,#6b7280);">(상위 ${k}개 · 하위 ${k}개 소재 기준 · Google Ads 종합점수)</span></div>
          <div class="signal-perf-block">
            <div class="signal-perf-card">
              <h4>🏆 최우수 소재 공통 강점</h4>
              <ul>${li(topStr, k)}</ul>
            </div>
            <div class="signal-perf-card">
              <h4>⚠️ 하위 소재 공통 약점</h4>
              <ul>${li(botWk, k)}</ul>
            </div>
          </div>
        </div>`;
    }
```

- [ ] **Step 2: renderCrossPerformanceCards 연결**

`container.innerHTML = crossHtml + perfHtml;`(6072) 교체:
```js
      const perfTagHtml = buildPerfTagAnalysis(creatives || window.currentCreatives || []);
      container.innerHTML = crossHtml + perfHtml + perfTagHtml;
```

- [ ] **Step 3: preview 검증 — 반환·숨김·DOM·불변**

`step1_integrated.html?_=<ts>` → gd 분석(calculateScores) 후 `preview_eval`:
```js
(function(){
  var cs = window.currentCreatives || [];
  var scoreSum = cs.reduce(function(a,c){ return a + (Number(c.TotalScore)||0); }, 0);
  var html = buildPerfTagAnalysis(cs);
  // 숨김 케이스
  var emptyFew = buildPerfTagAnalysis(cs.slice(0,3));           // n<4 → ''
  var emptyNoTag = buildPerfTagAnalysis([{_ads:{imp:1},TotalScore:9,meta:{}},{_ads:{imp:1},TotalScore:8,meta:{}},{_ads:{imp:1},TotalScore:7,meta:{}},{_ads:{imp:1},TotalScore:6,meta:{}}]);
  // DOM 존재 (renderCrossPerformanceCards 가 이미 렌더했거나, 강제 재렌더)
  var domHas = (document.getElementById('crossPerformanceContainer')||{}).textContent
    ? document.getElementById('crossPerformanceContainer').textContent.indexOf('성과별 강·약점 태그') !== -1 : false;
  var scoreSum2 = cs.reduce(function(a,c){ return a + (Number(c.TotalScore)||0); }, 0);
  return {
    n: cs.length,
    htmlNonEmpty: html.length > 0,
    hasTitle: html.indexOf('성과별 강·약점 태그') !== -1,
    hasTopCard: html.indexOf('최우수 소재 공통 강점') !== -1,
    hasBotCard: html.indexOf('하위 소재 공통 약점') !== -1,
    hiddenWhenFew: emptyFew === '',
    hiddenWhenNoTag: emptyNoTag === '',
    domHas: domHas,
    scoreUnchanged: (scoreSum === scoreSum2)
  };
})()
```
Expected: `htmlNonEmpty:true`, `hasTitle/hasTopCard/hasBotCard:true`, `hiddenWhenFew:true`, `hiddenWhenNoTag:true`, `scoreUnchanged:true`. `domHas` 는 전체 성과 요약 탭이 렌더된 상태면 true(아니면 탭 전환 후 재확인). `preview_console_logs`(error) → 없음. 가능하면 `preview_screenshot`로 2카드 시각 확인.

- [ ] **Step 4: Commit**
```bash
git add step1_integrated.html
git commit -m "feat(step1): ⑧ 전체 성과 요약 — 성과별 강·약점 태그 2카드"
```

---

## 실행 메모
- 인라인 변경 → pytest 무관. 검증은 preview `?_=<ts>` 로드·gd 분석 후 buildPerfTagAnalysis 단언 + DOM.
- gd 8소재 전부 태그 보유 시 정상 표시. DOM 확인은 '전체 성과 요약' 탭 활성 필요할 수 있음(switchMainTab('summary')).
