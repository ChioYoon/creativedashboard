# MMP 웹 사전예약 전환 — Phase 2 (스코어링) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 등록 기준 타이틀(ZEUS)의 MMP 종합점수(`mmp_quality_score`)를 **사전예약 지표세트**(전환=등록·CPA·등록IPM 3축; ROAS·D1잔존 제외)로 산출한다. 설치 기준 타이틀은 기존 4축 무변경.

**Architecture:** Phase 1이 채운 `mmp_conversions`를 스코어 전환 축으로 쓴다. `compute_mmp_quality`/`compute_mmp_quality_scores`에 `conversion_basis` 인자를 추가해 registration 분기(3축)를 넣고, `inject_mmp_into_records`가 cfg 기반 basis를 전달. 방향성은 CPA↓·IPM↑로 기존 `d1_cpi`/`d1_ipm` 키·랭크 로직을 재사용(등록 basis에선 그 키가 CPA·등록IPM을 담음).

**Tech Stack:** Python 3, pytest. `pipeline/mmp_metrics.py`, `pipeline/main.py`.

## Global Constraints

- 등록 basis 지표: 전환=`conversions`(등록)↑, CPA=`cost/conversions`↓, 등록IPM=`conversions/impressions×1000`↑. **ROAS·D1잔존 = None(축 제외)** → 3축 균등.
- 키 재사용: 등록 basis에서 `compute_mmp_quality` 반환의 `d1_cpi`=CPA, `d1_ipm`=등록IPM(랭크 방향 동일 → `compute_mmp_quality_scores`의 `d1_cpi`↓·`d1_ipm`↑ 로직 그대로). `d1_retention`·`d7_roas`=None.
- **단일 스위치**: `conversion_basis`는 cfg `airbridge_conversion_metric` 설정 유무로 결정(설정=`"registration"`, 미설정=`"install"`). Phase 1과 동일 스위치.
- 하위호환: `conversion_basis` 기본 `"install"` → 기존 4축 로직·기존 테스트 무변경.
- `mmp_quality_score` 출력에 `convBasis`("사전예약"|"설치") 추가(Phase 3 라벨용). 등록 basis는 `roas` 점수 키 None.
- 시크릿 미포함. `main` 브랜치. 각 Task 커밋, push는 사용자 승인 후.
- 테스트: `pytest -q` 전체 통과. 실행 = `PYTHONUTF8=1 PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m pytest`.

## 현재 상태(참고)

- `compute_mmp_quality(agg)`(mmp_metrics.py:51-64): impressions/cost/installs/retained_d1/revenue_d7 → `{d1_ipm, d1_cpi, d1_retention, d7_roas}`.
- `compute_mmp_quality_scores(metrics_by_creative)`(67-111): `rank_score("installs",True/"d1_cpi",False/"d1_ipm",True/"d7_roas",True)` 4축 균등 → `{total,grade,rank,conv,cpi,ipm,roas}`. `_assign_rank_with_ties` 사용.
- `inject_mmp_into_records(records, mmp_daily, source_name, currency, fx_rate, aliases)`(main.py:259): 루프 `a=aggregate_rows_total(rows)`(282) → `q=compute_mmp_quality(a)`(283) → `r.mmp_*` 저장(284-293, `r.mmp_conversions=a["conversions"]` Phase1) → `scored_metrics={cid:{installs,d1_cpi,d1_ipm,d7_roas}}`(295-299) → `compute_mmp_quality_scores`(302). 호출부 main.py:1092-1094(cfg 보유).
- Phase 1: cfg에 `airbridge_conversion_metric`, record에 `mmp_conversions`, aggregate에 `conversions`.

---

### Task 1: compute_mmp_quality — registration 분기

**Files:**
- Modify: `pipeline/mmp_metrics.py` (`compute_mmp_quality` 51-64)
- Test: `tests/test_mmp_quality_registration.py` (create)

**Interfaces:**
- Produces: `compute_mmp_quality(agg, conversion_basis="install")` — registration이면 `{d1_ipm=등록IPM, d1_cpi=CPA, d1_retention=None, d7_roas=None}`.

- [ ] **Step 1: 실패 테스트 작성**

Create `tests/test_mmp_quality_registration.py`:
```python
from pipeline.mmp_metrics import compute_mmp_quality


def test_registration_basis_uses_conversions():
    agg = {"impressions": 10000, "cost": 20000, "conversions": 100,
           "installs": 0, "retained_d1": 0, "revenue_d7": 0}
    q = compute_mmp_quality(agg, conversion_basis="registration")
    assert q["d1_ipm"] == (100 / 10000) * 1000      # 등록 IPM = 10.0
    assert q["d1_cpi"] == 20000 / 100               # CPA = 200
    assert q["d1_retention"] is None                 # 웹 사전예약 N/A
    assert q["d7_roas"] is None                      # N/A


def test_registration_zero_denominators_none():
    q = compute_mmp_quality({"impressions": 0, "cost": 0, "conversions": 0}, conversion_basis="registration")
    assert q["d1_ipm"] is None
    assert q["d1_cpi"] is None


def test_install_basis_unchanged_default():
    agg = {"impressions": 10000, "cost": 20000, "installs": 50, "retained_d1": 40, "revenue_d7": 60000}
    q = compute_mmp_quality(agg)     # 기본 install
    assert q["d1_ipm"] == (40 / 10000) * 1000
    assert q["d1_cpi"] == 20000 / 40
    assert q["d7_roas"] == 60000 / 20000
```

- [ ] **Step 2: 실패 확인**

Run: `PYTHONUTF8=1 .venv/Scripts/python.exe -m pytest tests/test_mmp_quality_registration.py -q`
Expected: FAIL (`compute_mmp_quality`가 `conversion_basis` 인자 없음).

- [ ] **Step 3: 구현**

`compute_mmp_quality`(mmp_metrics.py:51-64) 전체 교체:
```python
def compute_mmp_quality(agg: dict, conversion_basis: str = "install") -> dict:
    """한 소재의 집계 dict → 4 품질지표. 0 분모는 None (cost·impressions=0 → '—').

    conversion_basis='registration'(웹 사전예약): 전환축을 등록(conversions)으로.
      d1_ipm=등록IPM(등록/노출×1000), d1_cpi=CPA(비용/등록), d1_retention·d7_roas=None(N/A).
    'install'(기본): 기존 설치 기반 4지표.
    """
    impressions = agg.get("impressions", 0)
    cost = agg.get("cost", 0)

    if conversion_basis == "registration":
        conversions = agg.get("conversions", 0)
        reg_ipm: Optional[float] = (conversions / impressions) * 1000 if impressions > 0 else None
        cpa: Optional[float] = (cost / conversions) if (conversions > 0 and cost > 0) else None
        return {"d1_ipm": reg_ipm, "d1_cpi": cpa, "d1_retention": None, "d7_roas": None}

    installs = agg.get("installs", 0)
    retained_d1 = agg.get("retained_d1", 0)
    revenue_d7 = agg.get("revenue_d7", 0)

    d1_ipm: Optional[float] = (retained_d1 / impressions) * 1000 if impressions > 0 else None
    d1_cpi: Optional[float] = (cost / retained_d1) if (retained_d1 > 0 and cost > 0) else None
    d1_retention = (retained_d1 / installs) * 100 if installs > 0 else 0.0
    d7_roas: Optional[float] = (revenue_d7 / cost) if cost > 0 else None

    return {"d1_ipm": d1_ipm, "d1_cpi": d1_cpi, "d1_retention": d1_retention, "d7_roas": d7_roas}
```

- [ ] **Step 4: 통과 확인**

Run: `PYTHONUTF8=1 .venv/Scripts/python.exe -m pytest tests/test_mmp_quality_registration.py -q`
Expected: PASS (3 passed)

- [ ] **Step 5: 커밋**

```bash
cd /c/claude/cloop_dashboard && git add pipeline/mmp_metrics.py tests/test_mmp_quality_registration.py && git commit -m "feat(pipeline): compute_mmp_quality registration 분기(등록·CPA·IPM, ROAS/잔존 제외) (MMP 전환 Phase2)"
```

---

### Task 2: compute_mmp_quality_scores — registration 3축

**Files:**
- Modify: `pipeline/mmp_metrics.py` (`compute_mmp_quality_scores` 67-111)
- Test: `tests/test_mmp_scores_registration.py` (create)

**Interfaces:**
- Consumes: metrics dict에 등록 basis는 `conversions,d1_cpi,d1_ipm`.
- Produces: `compute_mmp_quality_scores(metrics_by_creative, conversion_basis="install")` — registration이면 3축(conversions↑·d1_cpi↓·d1_ipm↑) 균등, 출력에 `convBasis`.

- [ ] **Step 1: 실패 테스트 작성**

Create `tests/test_mmp_scores_registration.py`:
```python
from pipeline.mmp_metrics import compute_mmp_quality_scores


def test_registration_scores_3axis_and_convbasis():
    metrics = {
        "a": {"conversions": 300, "d1_cpi": 100.0, "d1_ipm": 20.0},   # 전환↑ CPA↓ IPM↑ = 최상
        "b": {"conversions": 100, "d1_cpi": 300.0, "d1_ipm": 5.0},    # 최하
    }
    out = compute_mmp_quality_scores(metrics, conversion_basis="registration")
    assert out["a"]["total"] > out["b"]["total"]
    assert out["a"]["convBasis"] == "사전예약"
    assert out["a"]["rank"] == 1
    # 3축 균등: a는 세 축 모두 1위 → total 100
    assert out["a"]["total"] == 100.0
    # roas 축 없음 → roas 점수 None
    assert out["a"]["roas"] is None


def test_install_scores_unchanged_default():
    metrics = {
        "a": {"installs": 100, "d1_cpi": 100.0, "d1_ipm": 20.0, "d7_roas": 2.0},
        "b": {"installs": 50, "d1_cpi": 200.0, "d1_ipm": 10.0, "d7_roas": 1.0},
    }
    out = compute_mmp_quality_scores(metrics)      # 기본 install
    assert out["a"]["total"] == 100.0              # 4축 모두 1위
    assert out["a"]["convBasis"] == "설치"
    assert out["a"]["roas"] == 100.0
```

- [ ] **Step 2: 실패 확인**

Run: `PYTHONUTF8=1 .venv/Scripts/python.exe -m pytest tests/test_mmp_scores_registration.py -q`
Expected: FAIL (`conversion_basis` 인자 없음 / `convBasis` 키 없음).

- [ ] **Step 3: 구현**

`compute_mmp_quality_scores`(mmp_metrics.py:67-111) 전체 교체:
```python
def compute_mmp_quality_scores(metrics_by_creative: dict, conversion_basis: str = "install") -> dict:
    """소재별 지표 dict → 품질 종합점수 {total, grade, rank, convBasis, conv/cpi/ipm/roas 점수}.

    install(기본): 4축(전환=installs↑·D1 CPI↓·D1 IPM↑·D7 ROAS↑) 균등 25%.
    registration(웹 사전예약): 3축(전환=conversions↑·CPA(d1_cpi)↓·등록IPM(d1_ipm)↑) 균등 1/3. ROAS 제외.
    None 지표는 해당 축 점수 0(최하). rank 점수 = (n-rank+1)/n×100.
    """
    keys = list(metrics_by_creative.keys())
    n = len(keys)
    if n == 0:
        return {}
    items = [{"key": k, **metrics_by_creative[k]} for k in keys]

    def rank_score(field: str, higher_better: bool):
        def val(it):
            v = it.get(field)
            if v is None:
                return float("-inf") if higher_better else float("inf")
            return v
        ordered = sorted(items, key=val, reverse=higher_better)
        _assign_rank_with_ties(ordered, val)
        for it in ordered:
            none_v = it.get(field) is None
            it[f"_s_{field}"] = 0.0 if none_v else ((n - it["_assignedRank"] + 1) / n) * 100

    is_reg = conversion_basis == "registration"
    conv_field = "conversions" if is_reg else "installs"
    rank_score(conv_field, True)
    rank_score("d1_cpi", False)
    rank_score("d1_ipm", True)
    if not is_reg:
        rank_score("d7_roas", True)

    for it in items:
        if is_reg:
            it["_total"] = (it[f"_s_{conv_field}"] + it["_s_d1_cpi"] + it["_s_d1_ipm"]) / 3
        else:
            it["_total"] = (it[f"_s_{conv_field}"] + it["_s_d1_cpi"] + it["_s_d1_ipm"] + it["_s_d7_roas"]) / 4

    ranked = sorted(items, key=lambda it: it["_total"], reverse=True)
    out = {}
    for i, it in enumerate(ranked):
        t = it["_total"]
        grade = ("최우수" if t >= 80 else "우수" if t >= 60 else "양호" if t >= 40 else "보통" if t >= 20 else "개선필요")
        out[it["key"]] = {
            "total": round(t, 2), "grade": grade, "rank": i + 1,
            "convBasis": "사전예약" if is_reg else "설치",
            "conv": round(it[f"_s_{conv_field}"], 1), "cpi": round(it["_s_d1_cpi"], 1),
            "ipm": round(it["_s_d1_ipm"], 1),
            "roas": None if is_reg else round(it["_s_d7_roas"], 1),
        }
    return out
```

- [ ] **Step 4: 통과 확인**

Run: `PYTHONUTF8=1 .venv/Scripts/python.exe -m pytest tests/test_mmp_scores_registration.py -q`
Expected: PASS (2 passed)

> 기존 `compute_mmp_quality_scores` 테스트가 `roas` 키를 float로 단정하면(설치 경로) 여전히 통과(설치는 `roas`=float 유지). 전체 회귀는 Task 3에서.

- [ ] **Step 5: 커밋**

```bash
cd /c/claude/cloop_dashboard && git add pipeline/mmp_metrics.py tests/test_mmp_scores_registration.py && git commit -m "feat(pipeline): compute_mmp_quality_scores registration 3축 + convBasis (MMP 전환 Phase2)"
```

---

### Task 3: inject_mmp_into_records 배선 — basis 전달

**Files:**
- Modify: `pipeline/main.py` (`inject_mmp_into_records` 시그니처 259 + 본문 283·295-302, 호출부 1092-1094)
- Test: `tests/test_inject_mmp_basis.py` (create)

**Interfaces:**
- Consumes: `compute_mmp_quality(a, conversion_basis)`(Task1), `compute_mmp_quality_scores(scored, conversion_basis)`(Task2), `r.mmp_conversions`(Phase1).
- Produces: `inject_mmp_into_records(..., conversion_basis="install")`; 등록 basis면 scored_metrics에 `conversions` 사용.

- [ ] **Step 1: 실패 테스트 작성**

Create `tests/test_inject_mmp_basis.py`:
```python
from pipeline.schemas import CreativeRecord, CreativeMmpDaily
from pipeline.main import inject_mmp_into_records


def _rec(cid):
    return CreativeRecord(creative_id=cid, 소재명=cid, 파일명=f"{cid}.jpg", 유형="VID")


def test_registration_basis_scores_from_conversions():
    # 소재 2개, 등록 기준 — 전환(등록) 많은 쪽이 높은 점수
    recs = [_rec("P-Slogan-A"), _rec("P-Slogan-B")]
    daily = [
        CreativeMmpDaily(creative_name="P-Slogan-A", date="2026-07-01", channel="meta",
                         campaign_name="X_NU-Pre_Y", impressions=10000, cost=20000, conversions=300),
        CreativeMmpDaily(creative_name="P-Slogan-B", date="2026-07-01", channel="meta",
                         campaign_name="X_NU-Pre_Y", impressions=10000, cost=20000, conversions=100),
    ]
    inject_mmp_into_records(recs, daily, source_name="airbridge", conversion_basis="registration")
    a = next(r for r in recs if r.creative_id == "P-Slogan-A")
    b = next(r for r in recs if r.creative_id == "P-Slogan-B")
    assert a.mmp_conversions == 300 and b.mmp_conversions == 100
    assert a.mmp_quality_score is not None
    assert a.mmp_quality_score["convBasis"] == "사전예약"
    assert a.mmp_quality_score["total"] >= b.mmp_quality_score["total"]
```
> `CreativeRecord`/`CreativeMmpDaily` 필수 필드가 위와 다르면 에러 메시지 기준으로 생성 인자 보정(테스트 의도=등록 기반 점수 산출만). `inject_mmp_into_records`가 concept join에 `creative_id`/`소재명`을 쓰므로 creative_name과 creative_id가 join되도록 값 맞춤(안 되면 소재명=creative_name으로 설정).

- [ ] **Step 2: 실패 확인**

Run: `PYTHONUTF8=1 .venv/Scripts/python.exe -m pytest tests/test_inject_mmp_basis.py -q`
Expected: FAIL (`inject_mmp_into_records`에 `conversion_basis` 인자 없음).

- [ ] **Step 3: 구현**

(a) 시그니처(main.py:259)에 `conversion_basis="install"` 추가:
```python
def inject_mmp_into_records(records, mmp_daily, source_name="airbridge", currency=None, fx_rate=None, aliases=None, conversion_basis="install"):
```

(b) 품질 산출(283)에 basis 전달. 찾기:
```python
        q = compute_mmp_quality(a)
```
교체:
```python
        q = compute_mmp_quality(a, conversion_basis)
```

(c) scored_metrics(295-299)를 basis별로. 찾기:
```python
    scored_metrics = {
        r.creative_id: {"installs": r.mmp_installs, "d1_cpi": r.mmp_d1_cpi,
                        "d1_ipm": r.mmp_d1_ipm, "d7_roas": r.mmp_d7_roas}
        for r in records if r.mmp_source
    }
    if scored_metrics:
        from .mmp_metrics import compute_mmp_quality_scores
        qscores = compute_mmp_quality_scores(scored_metrics)
```
교체:
```python
    if conversion_basis == "registration":
        scored_metrics = {
            r.creative_id: {"conversions": r.mmp_conversions or 0,
                            "d1_cpi": r.mmp_d1_cpi, "d1_ipm": r.mmp_d1_ipm}
            for r in records if r.mmp_source
        }
    else:
        scored_metrics = {
            r.creative_id: {"installs": r.mmp_installs, "d1_cpi": r.mmp_d1_cpi,
                            "d1_ipm": r.mmp_d1_ipm, "d7_roas": r.mmp_d7_roas}
            for r in records if r.mmp_source
        }
    if scored_metrics:
        from .mmp_metrics import compute_mmp_quality_scores
        qscores = compute_mmp_quality_scores(scored_metrics, conversion_basis)
```

(d) 호출부(main.py:1092-1094)에 basis 전달. `inject_mmp_into_records(` 호출 인자에 추가:
```python
            conversion_basis=("registration" if cfg.get("airbridge_conversion_metric") else "install"),
```
> 정확 위치는 `inject_mmp_into_records(` 호출을 grep 해 기존 인자들(source_name/currency/fx_rate/aliases) 옆에 keyword 인자로 추가.

- [ ] **Step 4: 통과 확인 + 전체 회귀**

Run: `PYTHONUTF8=1 .venv/Scripts/python.exe -m pytest tests/test_inject_mmp_basis.py -q`
Expected: PASS
Run: `PYTHONUTF8=1 PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m pytest -q`
Expected: 전체 통과(설치 경로·기존 mmp 테스트 무변경).

- [ ] **Step 5: 커밋**

```bash
cd /c/claude/cloop_dashboard && git add pipeline/main.py tests/test_inject_mmp_basis.py && git commit -m "feat(pipeline): inject_mmp_into_records conversion_basis 배선(등록 기준 스코어) (MMP 전환 Phase2)"
```

---

## Self-Review

**1. Spec 커버리지 (Phase 2 = 스코어링):**
- 등록 기준 지표(전환·CPA·IPM, ROAS/잔존 제외) → Task 1(compute_mmp_quality registration). ✅
- 3축 종합점수 → Task 2(compute_mmp_quality_scores registration). ✅
- convBasis 표기 → Task 2 출력. ✅
- basis 배선(cfg 단일 스위치) → Task 3(inject + 호출부). ✅
- 하위호환(설치 4축 무변경) → 기본 install 분기·기존 테스트, Task 3 Step 4 전체 회귀. ✅

**2. Placeholder 스캔:** TBD 없음. 호출부(3d)는 grep 후 keyword 인자 추가로 명시(정확 코드 제시). ✅

**3. 타입 정합성:**
- `compute_mmp_quality(agg, conversion_basis)` ↔ inject 호출(Task3 b) ↔ 테스트 일치. ✅
- registration `d1_cpi`=CPA·`d1_ipm`=등록IPM → scored_metrics 등록 분기가 같은 키 사용(Task3 c) → scores registration 축(d1_cpi↓·d1_ipm↑) 일치. ✅
- `compute_mmp_quality_scores(..., conversion_basis)` ↔ inject 호출 ↔ 테스트 일치. `convBasis`/`roas=None`(reg) 정합. ✅
- `r.mmp_conversions`(Phase1 Optional[int]) → scored `conversions`(or 0) → conversions 축. ✅

## Out of Scope (이 Phase)

- 프런트 MMP 레이어 전환=등록 표시·convBasis 라벨 → **Phase 3**.
- mmp_d1_cpi/ipm 필드를 등록용 별도 필드로 분리(현재 키 재사용) → 불필요(방향 동일, Phase 3가 convBasis로 라벨).
- Phase 1 코스메틱(aggregate_creative_mmp docstring·미사용 import) — Phase 3 또는 별도 정리.
- 라이브 파이프라인 실행·zeus.json 재생성 → 사용자.
