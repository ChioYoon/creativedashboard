# AI 인사이트 무료 winning 카드 구조화 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 무료 플랜의 단일 `winning` AI 인사이트 슬롯을 평평한 텍스트에서 위닝(초록)/저효율(주황) 영역 + 소재행(IPM·CPA 칩) + 확장/중단 배지로 구조화 렌더한다.

**Architecture:** 순수 파서 `parseFreeWinning(text)`가 무료 winning 텍스트를 구조 객체로 변환 → 렌더러 `_buildFreeWinningCard(parsed)`가 구조화 HTML 생성 → `buildScoringSlotCards`의 `isWinningOnly` 분기에서만 호출(파싱 실패 시 기존 `_slotText` 평평 렌더로 폴백). 신규 CSS 클래스는 `css/gemini-ui.css`에 추가하며 기존 클래스는 건드리지 않는다.

**Tech Stack:** 바닐라 JS(브라우저), CSS. `js/gemini-api.js`, `css/gemini-ui.css`. 빌드·번들러 없음(정적 HTML이 직접 로드). JS 테스트 러너 없음 → 검증은 preview 서버 + `preview_eval`.

## Global Constraints

- 데이터·프롬프트·파이프라인 **변경 금지**. 렌더 레이어(`js/gemini-api.js`, `css/gemini-ui.css`)만.
- **유료 5슬롯·피로도 카드 렌더 변경 금지** — 현행 `_slotText`/`buildScoringSlotCards` 비-winning-only 경로 그대로.
- **신규 클래스만 추가** — 기존 `.gai-slot-card`, `.gai-bullet` 등 수정 금지(유료·피로도 외관 보존).
- **XSS**: 모든 사용자/Gemini 유래 텍스트(소재명·이유·콜아웃·권고명)는 출력 전 `_esc()`로 `& < > " '` 이스케이프.
- **graceful fallback**: 파싱 `_ok:false`면 무료 카드를 기존 `_slotText(slots.winning)` 평평 렌더로 폴백.
- 색: 위닝 `#10b981`, 저효율 `#f59e0b`, 중단 빨강 `#dc2626`(기존 테마). 신규 CSS는 기존 `css/gemini-ui.css` 컨벤션(색·radius·여백) 따른다.
- 검증은 preview 서버(`cloop-static`, 포트 8765) + `preview_eval`/screenshot. 파서는 순수함수라 입출력 단언.

---

### Task 1: 파서 `parseFreeWinning(rawText)`

무료 winning 텍스트 → 구조 객체. 순수함수(브라우저 전역 의존 없음).

**Files:**
- Modify: `js/gemini-api.js` — `parseScoringSlots` 함수 정의(약 886행) 바로 위 또는 아래에 `parseFreeWinning` 추가.

**Interfaces:**
- Consumes: 없음(문자열 입력).
- Produces: `parseFreeWinning(rawText: string) -> { win: {items: Array<{name, ipm, cpa, reason}>, callout: {label,text}|null}, lose: {items, callout}, recos: Array<{kind:'확장'|'중단', name}>, _ok: boolean }`. Task 2의 렌더러가 소비.

**입력 포맷(현행 무료 프롬프트):**
```
**[위닝 소재]**
• **소재명** (IPM X.XXX / CPA X,XXX) — 이유
**공통 패턴**: ...
**[저효율 소재]**
• **소재명** (IPM X.XXX / CPA X,XXX) — 원인
**위닝 대비 차이**: ...
**즉시 권고**: [확장 🟢] 소재명 / [중단 🔴] 소재명
```

- [ ] **Step 1: 구현 — `parseFreeWinning` 추가**

`js/gemini-api.js`에 추가:

```js
/**
 * 무료 winning 슬롯 텍스트 → 구조 객체 (순수함수).
 * 실패 시 _ok:false → 호출측에서 기존 평평 렌더로 폴백.
 */
function parseFreeWinning(rawText) {
  const empty = { win: { items: [], callout: null }, lose: { items: [], callout: null }, recos: [], _ok: false };
  if (!rawText || String(rawText).trim() === '') return empty;
  const text = String(rawText).replace(/\\n/g, '\n');

  // ── 구간 분할: [위닝 소재] / [저효율 소재] / 즉시 권고 ──
  const headerKind = (line) => {
    const h = line.replace(/\*/g, '').trim();
    if (/^\[?\s*위닝\s*소재/.test(h)) return 'win';
    if (/^\[?\s*저효율\s*소재/.test(h)) return 'lose';
    if (/^즉시\s*권고/.test(h)) return 'reco';
    return null;
  };
  const buf = { win: [], lose: [], reco: [] };
  let cur = null;
  for (const line of text.split('\n')) {
    const k = headerKind(line);
    if (k) {
      cur = k;
      if (k === 'reco') {  // "**즉시 권고**: ..." 헤더 줄에 내용이 붙은 경우
        const after = line.split(/[:：]/).slice(1).join(':').trim();
        if (after) buf.reco.push(after);
      }
      continue;
    }
    if (cur) buf[cur].push(line);
  }

  const ITEM_RE = /^[•\-▸▪◆]\s*\*\*(.+?)\*\*\s*(?:\(([^)]*)\))?\s*(?:[—:\-]\s*(.*))?$/;
  const CALLOUT_RE = /^\*\*(.+?)\*\*\s*[:：]\s*(.*)$/;
  const parseSection = (arr) => {
    const items = []; let callout = null;
    for (const raw of arr) {
      const line = raw.trim();
      if (!line) continue;
      const im = line.match(ITEM_RE);
      if (im) {
        const metrics = im[2] || '';
        const ipm = (metrics.match(/IPM\s*([\d.]+)/i) || [])[1] || null;
        const cpa = (metrics.match(/CPA\s*([\d,]+)/i) || [])[1] || null;
        items.push({ name: im[1].trim(), ipm, cpa, reason: (im[3] || '').trim() });
        continue;
      }
      if (!callout) {
        const cm = line.match(CALLOUT_RE);
        if (cm) callout = { label: cm[1].trim(), text: cm[2].trim() };
      }
    }
    return { items, callout };
  };

  const win = parseSection(buf.win);
  const lose = parseSection(buf.lose);

  // ── 즉시 권고: [확장 …] 소재 / [중단 …] 소재 ──
  const recoText = buf.reco.join(' ');
  const recos = [];
  let m;
  const EXP_RE = /\[\s*확장[^\]]*\]\s*([^/\n\[]+)/g;
  const STOP_RE = /\[\s*중단[^\]]*\]\s*([^/\n\[]+)/g;
  while ((m = EXP_RE.exec(recoText))) recos.push({ kind: '확장', name: m[1].trim() });
  while ((m = STOP_RE.exec(recoText))) recos.push({ kind: '중단', name: m[1].trim() });

  return { win, lose, recos, _ok: win.items.length > 0 };
}
```

- [ ] **Step 2: 검증 픽스처 준비 — 정상 입력 단언이 실패하는지 먼저 확인(red)**

구현 전 상태라면 preview에서 `parseFreeWinning`가 정의되지 않아 실패해야 한다. preview 서버 기동 후 step1 로드(`http://localhost:8765/step1_integrated.html`), 그다음 `preview_eval`:

```js
(function(){ return typeof parseFreeWinning; })()
```
Expected(구현 전): `"undefined"` → red. (구현·새로고침 후 `"function"`.)

- [ ] **Step 3: 정상 입력 파싱 단언(green)**

`preview_eval` (페이지 새로고침 후):

```js
(function(){
  var t = "**[위닝 소재]**\n• **제우스_검술액션_15s** (IPM 3.21 / CPA 4,100) — 전투 타격감 강조\n• **제우스_보스레이드_15s** (IPM 2.98 / CPA 4,520) — 보스 스케일 압도\n**공통 패턴**: 초반 3초 전투 클로즈업\n**[저효율 소재]**\n• **제우스_스토리_30s** (IPM 0.92 / CPA 11,200) — 도입부 정적\n**위닝 대비 차이**: 초반 타격 임팩트 부재\n**즉시 권고**: [확장 🟢] 검술액션 / [중단 🔴] 스토리_30s";
  var p = parseFreeWinning(t);
  return { ok:p._ok, winN:p.win.items.length, ipm0:p.win.items[0].ipm, cpa0:p.win.items[0].cpa,
           winCallout:p.win.callout&&p.win.callout.label, loseN:p.lose.items.length,
           loseCallout:p.lose.callout&&p.lose.callout.label, recos:p.recos.map(function(r){return r.kind+':'+r.name;}) };
})()
```
Expected:
```json
{ "ok":true, "winN":2, "ipm0":"3.21", "cpa0":"4,100", "winCallout":"공통 패턴",
  "loseN":1, "loseCallout":"위닝 대비 차이", "recos":["확장:검술액션","중단:스토리_30s"] }
```

- [ ] **Step 4: 폴백·부분 케이스 단언**

```js
(function(){
  var none = parseFreeWinning("그냥 자유 서술 텍스트, 마커 없음");
  var winOnly = parseFreeWinning("**[위닝 소재]**\n• **A** (IPM 2.0 / CPA 3,000) — 좋음");
  return { none_ok:none._ok, winOnly_ok:winOnly._ok, winOnly_loseN:winOnly.lose.items.length };
})()
```
Expected: `{ "none_ok":false, "winOnly_ok":true, "winOnly_loseN":0 }`

- [ ] **Step 5: Commit**

```bash
git add js/gemini-api.js
git commit -m "feat(insight): 무료 winning 텍스트 구조화 파서 parseFreeWinning"
```

---

### Task 2: 렌더러 `_buildFreeWinningCard` + CSS + `buildScoringSlotCards` 연결

파싱 결과를 구조화 HTML로 렌더하고, 무료 분기에서만 사용(폴백 포함). CSS 신규 클래스 추가.

**Files:**
- Modify: `js/gemini-api.js` — `_esc` 헬퍼 + `_buildFreeWinningCard` 추가, `buildScoringSlotCards`(약 991~1066행) `isWinningOnly` 분기 연결.
- Modify: `css/gemini-ui.css` — 신규 클래스 추가(파일 끝에).

**Interfaces:**
- Consumes: Task 1의 `parseFreeWinning(text)` 반환 객체.
- Produces: `_buildFreeWinningCard(parsed) -> htmlString`. `buildScoringSlotCards`가 `isWinningOnly && parsed._ok`일 때 사용.

- [ ] **Step 1: `_esc` + `_buildFreeWinningCard` 구현**

`js/gemini-api.js`에 추가(파서 근처):

```js
function _esc(s) {
  return String(s == null ? '' : s)
    .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;').replace(/'/g, '&#39;');
}

function _buildFreeWinningCard(parsed) {
  const chip = (label, val, cls) => val
    ? `<span class="gai-chip ${cls}">${label} ${_esc(val)}</span>` : '';
  const itemRow = (it) => `
    <div class="gai-item-row">
      <span class="gai-item-name">${_esc(it.name)}</span>
      ${chip('IPM', it.ipm, 'gai-chip--ipm')}
      ${chip('CPA', it.cpa ? '₩' + it.cpa : '', 'gai-chip--cpa')}
      ${it.reason ? `<span class="gai-item-reason">${_esc(it.reason)}</span>` : ''}
    </div>`;
  const calloutHtml = (c, cls) => c
    ? `<div class="gai-callout ${cls}"><strong>${_esc(c.label)}</strong> · ${_esc(c.text)}</div>` : '';

  const winZone = `
    <div class="gai-zone gai-zone--win">
      <div class="gai-zone-header">🏆 위닝 소재</div>
      ${parsed.win.items.map(itemRow).join('')}
      ${calloutHtml(parsed.win.callout, 'gai-callout--win')}
    </div>`;
  const loseZone = (parsed.lose.items.length || parsed.lose.callout) ? `
    <div class="gai-zone gai-zone--lose">
      <div class="gai-zone-header">⚠️ 저효율 소재</div>
      ${parsed.lose.items.map(itemRow).join('')}
      ${calloutHtml(parsed.lose.callout, 'gai-callout--lose')}
    </div>` : '';
  const recoRow = parsed.recos.length ? `
    <div class="gai-reco-row">
      <span class="gai-reco-label">즉시 권고</span>
      ${parsed.recos.map(r => `<span class="gai-badge gai-badge--${r.kind === '확장' ? 'expand' : 'stop'}">${_esc(r.kind)} · ${_esc(r.name)}</span>`).join('')}
    </div>` : '';

  return `<div class="gai-structured-card">${winZone}${loseZone}${recoRow}</div>`;
}
```

- [ ] **Step 2: `buildScoringSlotCards`에 무료 분기 연결**

`js/gemini-api.js`의 `buildScoringSlotCards` 내부, `retryBtn` 계산 직후(약 1039행, `const cardHtmlArr = ...` 바로 위)에 삽입:

```js
  // 무료 단일 winning 슬롯: 구조화 렌더 시도, 실패 시 아래 일반(평평) 렌더로 폴백
  if (isWinningOnly) {
    const parsedFW = parseFreeWinning(slots.winning);
    if (parsedFW._ok) {
      return `${_buildFreeWinningCard(parsedFW)}
        <div class="gai-slot-summary"><span>${summaryParts.join(' ')}</span>${retryBtn}</div>`;
    }
    // _ok:false → 아래 기존 cardHtmlArr 경로로 폴백
  }
```

(이 블록은 `summaryParts`/`retryBtn` 정의 이후, 기존 `cardHtmlArr` 생성 이전에 위치해야 한다.)

- [ ] **Step 3: CSS 신규 클래스 추가**

`css/gemini-ui.css` 끝에 추가(기존 클래스 수정 없음):

```css
/* ── 무료 winning 구조화 카드 ── */
.gai-structured-card { border:1px solid #e5e7eb; border-radius:10px; overflow:hidden; background:#fff; }
.gai-zone { padding:12px 14px; }
.gai-zone--win  { background:#ecfdf5; }
.gai-zone--lose { background:#fffbeb; border-top:1px solid #e5e7eb; }
.gai-zone-header { font-size:13px; font-weight:700; margin-bottom:8px; }
.gai-zone--win  .gai-zone-header { color:#10b981; }
.gai-zone--lose .gai-zone-header { color:#f59e0b; }
.gai-item-row { display:flex; align-items:center; flex-wrap:wrap; gap:6px; margin-bottom:6px; font-size:12.5px; line-height:1.5; }
.gai-item-name { font-weight:600; color:#111827; }
.gai-item-reason { color:#6b7280; }
.gai-chip { font-size:11px; padding:1px 7px; border-radius:6px; border:1px solid #e5e7eb; background:#fff; white-space:nowrap; }
.gai-chip--ipm { color:#0f766e; }
.gai-chip--cpa { color:#6b7280; }
.gai-callout { font-size:12px; border-radius:6px; padding:6px 9px; margin-top:4px; background:#fff; }
.gai-callout--win  { color:#0f766e; }
.gai-callout--lose { color:#b45309; }
.gai-reco-row { display:flex; align-items:center; flex-wrap:wrap; gap:6px; padding:10px 14px; border-top:1px solid #e5e7eb; }
.gai-reco-label { font-size:12px; color:#6b7280; }
.gai-badge { font-size:12px; padding:2px 9px; border-radius:6px; }
.gai-badge--expand { background:#ecfdf5; color:#10b981; }
.gai-badge--stop   { background:#fef2f2; color:#dc2626; }
```

- [ ] **Step 4: 정상 렌더 검증(green)**

preview 서버에서 step1 로드 후 `preview_eval` — 무료 winning 합성 데이터로 렌더하고 box에 주입:

```js
(function(){
  var t = "**[위닝 소재]**\n• **제우스_검술액션_15s** (IPM 3.21 / CPA 4,100) — 전투 타격감 강조\n**공통 패턴**: 초반 3초 전투 클로즈업\n**[저효율 소재]**\n• **제우스_스토리_30s** (IPM 0.92 / CPA 11,200) — 도입부 정적\n**위닝 대비 차이**: 초반 타격 임팩트 부재\n**즉시 권고**: [확장 🟢] 검술액션 / [중단 🔴] 스토리_30s";
  var html = buildScoringSlotCards({ winning: t });
  var host = document.createElement('div'); host.id='__fwtest'; host.innerHTML = html; document.body.appendChild(host);
  return {
    hasWinZone: html.indexOf('gai-zone--win') !== -1,
    hasLoseZone: html.indexOf('gai-zone--lose') !== -1,
    hasIpmChip: html.indexOf('IPM 3.21') !== -1,
    hasCpaChip: html.indexOf('₩4,100') !== -1,
    hasExpandBadge: html.indexOf('gai-badge--expand') !== -1,
    hasStopBadge: html.indexOf('gai-badge--stop') !== -1
  };
})()
```
Expected: 모든 값 `true`. 이어서 `preview_screenshot`로 위닝(초록)/저효율(주황) 영역·칩·배지 시각 확인.

- [ ] **Step 5: 폴백 검증 — 마커 없는 텍스트는 평평 렌더**

```js
(function(){
  var html = buildScoringSlotCards({ winning: "마커 없는 자유 서술 위닝 인사이트입니다." });
  return { structured: html.indexOf('gai-structured-card') !== -1, hasSlotCard: html.indexOf('gai-slot-card') !== -1 };
})()
```
Expected: `{ "structured": false, "hasSlotCard": true }` (구조화 안 함 → 기존 카드로 폴백).

- [ ] **Step 6: XSS·잘림 검증**

```js
(function(){
  var t = "**[위닝 소재]**\n• **<img src=x onerror=alert(1)>** (IPM 1.0 / CPA 2,000) — \"위험\"&'테스트'";
  var html = buildScoringSlotCards({ winning: t });
  return { rawImg: html.indexOf('<img src=x onerror') !== -1, escImg: html.indexOf('&lt;img src=x onerror') !== -1 };
})()
```
Expected: `{ "rawImg": false, "escImg": true }` (raw 미주입·이스케이프).

- [ ] **Step 7: 잘림 배지 유지 검증**

잘림은 기존 `buildScoringSlotCards`의 `hasTruncated`→`summaryParts` 경로로 처리되며, 구조화 return이 `summaryParts`를 포함하므로 코드 추가 없이 보존된다. 확인:

```js
(function(){
  var t = "**[위닝 소재]**\n• **A** (IPM 2.0 / CPA 3,000) — 좋은 소재…(잘림)";
  var html = buildScoringSlotCards({ winning: t });
  return { structured: html.indexOf('gai-structured-card')!==-1, truncNotice: html.indexOf('잘렸습니다')!==-1 };
})()
```
Expected: `{ "structured": true, "truncNotice": true }`. 정리: `var n=document.getElementById('__fwtest'); if(n) n.remove();`

- [ ] **Step 8: 콘솔 에러 확인**

`preview_console_logs` (level error) → 출력 없음(에러 0) 확인.

- [ ] **Step 9: Commit**

```bash
git add js/gemini-api.js css/gemini-ui.css
git commit -m "feat(insight): 무료 winning 구조화 카드 렌더 + CSS (위닝/저효율 영역·칩·배지)"
```

---

## 실행 메모

- `parseFreeWinning`/`_buildFreeWinningCard`/`_esc`/`buildScoringSlotCards`는 모두 `js/gemini-api.js` 동일 파일 → 함수명 충돌 없는지 확인(현재 `_esc`·`parseFreeWinning`·`_buildFreeWinningCard` 미존재).
- 검증은 preview 서버(`cloop-static`) + `preview_eval`/screenshot/console — 컨트롤러가 구동. 각 Task 끝에서 새로고침 후 단언.
- 데이터·프롬프트 무변경이므로 pytest 영향 없음(파이프라인 테스트 재실행 불필요).
