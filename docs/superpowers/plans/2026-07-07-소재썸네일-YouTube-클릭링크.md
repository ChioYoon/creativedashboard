# 소재 썸네일 YouTube 대응 + 클릭 링크 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 세 HTML 추출물(요약 리포트·HTML 추출·제작 브리프)에서 영상(VID) 소재의 미리보기가 안 나오는 버그를 고치고, 이미지·영상 소재 모두 클릭 시 원본을 여는 링크를 추가한다.

**Architecture:** 근본 원인은 `이미지링크`(영상은 `youtube.com/watch?v=…` 웹페이지 URL)를 그대로 `<img src>`에 넣어 깨지는 것. 공용 헬퍼 `creativeThumbHtml(url, type, w, h)` 하나를 만들어 ① YouTube URL→`img.youtube.com/vi/{id}/hqdefault.jpg` 썸네일 변환, ② 이미지 URL→원본, ③ 둘 다 `<a target="_blank">` 클릭 링크, ④ 로드 실패 시 아이콘 폴백(뒤에 깔린 라벨을 `onerror`로 노출)을 자기완결 인라인 스타일로 반환. 네 개 렌더 사이트가 이 헬퍼를 호출하도록 교체. 전부 `step1_integrated.html` 인라인 JS.

**Tech Stack:** Vanilla JS(인라인), 자기완결 HTML Blob 다운로드. 신규 의존성 없음.

## Global Constraints

- 대상 파일은 `step1_integrated.html` 단일 파일만. 다른 파일 수정 금지.
- 브랜치: `main` 직접(프로젝트 컨벤션 = SDD on main).
- 헬퍼는 **한 번만 정의**(DRY). 네 사이트는 이 헬퍼를 호출만 함(로직 중복 금지).
- 영상 썸네일 = `https://img.youtube.com/vi/{VIDEO_ID}/hqdefault.jpg`. VIDEO_ID는 `watch?v=`, `youtu.be/`, `embed/`, `shorts/` 형태에서 추출(11자).
- 이미지·영상 **모두 클릭 시 새 탭**(`<a target="_blank" rel="noopener">`)으로 원본(영상=watch URL, 이미지=원본 URL) 열기.
- 로드 실패/URL 없음 → 아이콘(유형 라벨 'VID'/'IMG') 폴백. URL이 아예 없으면 링크 없는 아이콘 박스.
- XSS: 헬퍼 내 URL·라벨은 `esc()` 처리 후 삽입. `onerror`는 HTML 문자열 재조립 없이 `this.style.display='none'`만(따옴표 이스케이프 이슈 회피).
- 시크릿(.env·google_ads.yaml) 읽기/출력/커밋 금지.
- 검증: pytest는 파이썬 백엔드용(JS 무관) — 114 passed 유지 확인. 런타임 검증은 컨트롤러가 preview MCP로 수행(구현 subagent는 편집+정적grep+pytest+커밋만, preview 생략).
- 커밋: 각 Task 완료 시 `main`에 커밋. push는 사용자 명시 승인 후 별도(컨트롤러).

## 현재 상태(참고 · 착수 전 확인)

- **요약 리포트** `renderSummaryReportHtml`(step1_integrated.html): 두 곳에서 `이미지링크`를 그대로 img로 렌더.
  - moverRow: `<div class="tt">${m.thumb ? \`<img src="${esc(m.thumb)}" alt="">\` : esc(m.type)}</div>` (~5636)
  - topHtml: `<div class="tt">${t.thumb ? \`<img src="${esc(t.thumb)}" alt="">\` : esc(t.type)}</div>` (~5650)
  - `m.thumb`/`t.thumb`는 `이미지링크`(영상=youtube watch URL). `m.type`/`t.type`은 `유형`(VID/BNR).
- **HTML 추출** `exportResultHTML`(~5887 detailRows): `imageUrl`/`isVideo`/`hasImg` 선언 후 `previewCell` if/else(영상 썸네일 실패 시 'VID' 박스, onclick 링크; 이미지 `<a>` 링크). `imageUrl`/`isVideo`/`hasImg`는 previewCell 전용(이후 행 셀은 `v.*`·`c.유형`·`c.key`만 사용).
- **제작 브리프** `exportCreativeBrief`(~6123): `thumb = (c) => {…}` — `u` = `링크`/`이미지링크`/`image_url`, img onerror→숨김(폴백 없음), 링크 없음.

---

### Task 1: 공용 썸네일 헬퍼 `creativeThumbHtml` + 요약 리포트 배선

**Files:**
- Modify: `step1_integrated.html` — 헬퍼 신규(≈`buildSummaryReportData` 종료 뒤, `renderSummaryReportHtml` 앞), moverRow(~5636)·topHtml(~5650) 호출 교체.

**Interfaces:**
- Produces: `creativeThumbHtml(url, type, w, h)` → 자기완결 인라인 스타일 HTML 문자열(클릭 링크 포함 `<a>` 또는 링크 없는 아이콘 `<div>`). Task 2도 이 헬퍼를 재사용.

- [ ] **Step 1: 공용 헬퍼 삽입**

찾기 — `buildSummaryReportData` 종료부(반환문 + 닫는 `}`)와 렌더 함수 주석(유일 컨텍스트):
```js
      return { titleName, period, layer, kpis, topN, typeRows, insightHtml, comparison: comparison || null, narrative: narrative || '' };
    }

    // ── 성과 요약 리포트: HTML 렌더 (자체 완결·인쇄용) ──
```
교체(둘 사이에 헬퍼 삽입):
```js
      return { titleName, period, layer, kpis, topN, typeRows, insightHtml, comparison: comparison || null, narrative: narrative || '' };
    }

    // ── 소재 썸네일 셀(공용): 영상=YouTube 썸네일, 이미지=원본, 둘 다 클릭 링크(새 탭), 로드 실패 시 아이콘 폴백 ──
    function creativeThumbHtml(url, type, w, h) {
      const esc = s => String(s == null ? '' : s).replace(/[&<>"]/g, ch => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;' }[ch]));
      const u = String(url || '');
      const t = String(type || '').toUpperCase();
      const isVid = t === 'VID';
      const ym = u.match(/(?:youtube\.com\/(?:watch\?v=|embed\/|shorts\/)|youtu\.be\/)([\w-]{11})/);
      const yid = ym ? ym[1] : '';
      const isImg = /^https?:\/\//.test(u) && !yid;
      const box = `width:${w}px;height:${h}px;border-radius:6px;flex:none;`;
      const label = esc(t || (isVid ? 'VID' : 'IMG')) || '—';
      const fs = Math.max(9, Math.round(h / 4.5));
      const iconBox = `<div style="${box}background:#F4F2EF;display:flex;align-items:center;justify-content:center;font-size:${fs}px;font-weight:700;color:#999;">${label}</div>`;
      if (!yid && !isImg) return iconBox;
      const src = yid ? `https://img.youtube.com/vi/${yid}/hqdefault.jpg` : esc(u);
      const ph = `<div style="position:absolute;inset:0;display:flex;align-items:center;justify-content:center;font-size:${fs}px;font-weight:700;color:#999;">${label}</div>`;
      const img = `<img src="${src}" style="position:absolute;inset:0;width:100%;height:100%;object-fit:cover;" onerror="this.style.display='none'">`;
      const pd = Math.round(h * 0.4);
      const play = isVid ? `<div style="position:absolute;inset:0;display:flex;align-items:center;justify-content:center;pointer-events:none;"><div style="width:${pd}px;height:${pd}px;background:rgba(0,0,0,.55);border-radius:50%;display:flex;align-items:center;justify-content:center;color:#fff;font-size:${Math.round(pd * 0.55)}px;">▶</div></div>` : '';
      return `<a href="${esc(u)}" target="_blank" rel="noopener" title="${isVid ? '영상 열기' : '원본 이미지 열기'}" style="position:relative;display:block;${box}overflow:hidden;background:#F4F2EF;text-decoration:none;">${ph}${img}${play}</a>`;
    }

    // ── 성과 요약 리포트: HTML 렌더 (자체 완결·인쇄용) ──
```

- [ ] **Step 2: moverRow 썸네일 교체**

찾기(~5636):
```js
          <div class="tt">${m.thumb ? `<img src="${esc(m.thumb)}" alt="">` : esc(m.type)}</div>
```
교체:
```js
          ${creativeThumbHtml(m.thumb, m.type, 44, 44)}
```

- [ ] **Step 3: topHtml 썸네일 교체**

찾기(~5650):
```js
          <div class="tt">${t.thumb ? `<img src="${esc(t.thumb)}" alt="">` : esc(t.type)}</div>
```
교체:
```js
          ${creativeThumbHtml(t.thumb, t.type, 44, 44)}
```

- [ ] **Step 4: 정적 검증**

Run: `cd /c/claude/cloop_dashboard && grep -n "function creativeThumbHtml\|creativeThumbHtml(m.thumb\|creativeThumbHtml(t.thumb\|img.youtube.com/vi" step1_integrated.html`
Expected: 헬퍼 정의 1, moverRow 호출 1, topHtml 호출 1, youtube 썸네일 URL 1 — 총 4줄. `class="tt">${m.thumb`·`class="tt">${t.thumb` 는 더 이상 없어야 함(교체됨).

- [ ] **Step 5: 회귀 확인(pytest)**

Run: `cd /c/claude/cloop_dashboard && PYTHONUTF8=1 PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m pytest -q`
Expected: `114 passed`.

- [ ] **Step 6: 커밋**

```bash
cd /c/claude/cloop_dashboard && git add step1_integrated.html && git commit -m "fix(step1): 요약 리포트 영상 썸네일(YouTube)+클릭 링크, 공용 creativeThumbHtml 헬퍼"
```

> preview MCP 검증 금지 — 컨트롤러가 런타임 확인(요약 리포트 HTML에 youtube 썸네일 URL·`<a target=_blank>`·▶ 배지, 이미지 소재는 원본 링크, URL 없으면 아이콘).

---

### Task 2: HTML 추출 + 제작 브리프 배선(동일 헬퍼 재사용)

**Files:**
- Modify: `step1_integrated.html` — `exportResultHTML` previewCell 블록(~5888-5915) 교체, `exportCreativeBrief` thumb(~6123-6132) 교체.

**Interfaces:**
- Consumes: `creativeThumbHtml(url, type, w, h)` (Task 1 정의).

- [ ] **Step 1: HTML 추출 previewCell 교체 (imageUrl/isVideo/hasImg 제거 — previewCell 전용이라 안전)**

찾기(~5888-5915, `imageUrl` 선언부터 previewCell if/else 끝까지):
```js
        const imageUrl = c.meta?.['링크'] || c.이미지링크 || c.meta?.['image_url'] || '';
        const isVideo  = (c.유형||'').toUpperCase() === 'VID';
        const hasImg   = imageUrl && imageUrl.startsWith('http');

        // 미리보기 셀
        let previewCell = '';
        if (isVideo) {
          if (hasImg) {
            // 영상 링크가 있으면 썸네일+플레이 오버레이
            previewCell = `<div style="position:relative;width:80px;height:54px;display:inline-block;cursor:pointer;" onclick="window.open('${imageUrl}','_blank')">
              <img src="${imageUrl}" style="width:80px;height:54px;object-fit:cover;border-radius:6px;" onerror="this.parentNode.innerHTML='<div style=\\'width:80px;height:54px;background:#F4F2EF;border-radius:6px;display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:700;color:#666;\\'>VID</div>'">
              <div style="position:absolute;inset:0;display:flex;align-items:center;justify-content:center;">
                <div style="width:26px;height:26px;background:rgba(0,0,0,.55);border-radius:50%;display:flex;align-items:center;justify-content:center;">
                  <span style="color:#fff;font-size:12px;margin-left:2px;">▶</span>
                </div>
              </div>
            </div>`;
          } else {
            previewCell = `<div style="width:80px;height:54px;background:#F4F2EF;border-radius:6px;display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:700;color:#666;cursor:default;">VID</div>`;
          }
        } else if (hasImg) {
          previewCell = `<a href="${imageUrl}" target="_blank" title="원본 이미지 열기">
            <img src="${imageUrl}" style="width:80px;height:54px;object-fit:cover;border-radius:6px;display:block;"
                 onerror="this.parentNode.innerHTML='<div style=\\'width:80px;height:54px;background:#F4F2EF;border-radius:6px;display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:700;color:#999;\\'>IMG</div>'">
          </a>`;
        } else {
          previewCell = `<div style="width:80px;height:54px;background:#F4F2EF;border-radius:6px;display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:700;color:#999;">—</div>`;
        }
```
교체:
```js
        const previewCell = creativeThumbHtml(c.meta?.['링크'] || c.이미지링크 || c.meta?.['image_url'] || '', c.유형, 80, 54);
```

- [ ] **Step 2: 제작 브리프 thumb 교체**

찾기(~6123-6132):
```js
      const thumb = (c) => {
        const u = c.meta?.['링크'] || c.이미지링크 || c.meta?.['image_url'] || '';
        const isVid = (c.유형 || '').toUpperCase() === 'VID';
        const ok = u && u.startsWith('http');
        if (ok) {
          const play = isVid ? `<div style="position:absolute;inset:0;display:flex;align-items:center;justify-content:center;"><div style="width:24px;height:24px;background:rgba(0,0,0,.55);border-radius:50%;display:flex;align-items:center;justify-content:center;color:#fff;font-size:11px;">▶</div></div>` : '';
          return `<div style="position:relative;width:96px;height:64px;flex:none;"><img src="${u}" style="width:96px;height:64px;object-fit:cover;border-radius:8px;display:block;" onerror="this.style.display='none'">${play}</div>`;
        }
        return `<div style="width:96px;height:64px;background:#F4F2EF;border-radius:8px;flex:none;display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:700;color:#999;">${isVid ? 'VID' : 'IMG'}</div>`;
      };
```
교체:
```js
      const thumb = (c) => creativeThumbHtml(c.meta?.['링크'] || c.이미지링크 || c.meta?.['image_url'] || '', c.유형, 96, 64);
```

- [ ] **Step 3: 정적 검증**

Run: `cd /c/claude/cloop_dashboard && grep -n "creativeThumbHtml(c.meta\|const previewCell = creativeThumbHtml\|const thumb = (c) => creativeThumbHtml" step1_integrated.html`
Expected: previewCell 1 + thumb 1 = 2줄(각 `creativeThumbHtml(c.meta…` 포함). 구 previewCell if/else·구 thumb 블록은 사라져야 함.

- [ ] **Step 4: 잔여 원본-URL-직접-img 확인 (회귀 방지)**

Run: `cd /c/claude/cloop_dashboard && grep -n "onclick=\"window.open('\${imageUrl}\|<img src=\"\${u}\"\|<img src=\"\${imageUrl}\"" step1_integrated.html`
Expected: **0줄**(모든 직접 img 삽입이 헬퍼로 대체됨). 매치가 있으면 교체 누락.

- [ ] **Step 5: 회귀 확인(pytest)**

Run: `cd /c/claude/cloop_dashboard && PYTHONUTF8=1 PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m pytest -q`
Expected: `114 passed`.

- [ ] **Step 6: 커밋**

```bash
cd /c/claude/cloop_dashboard && git add step1_integrated.html && git commit -m "fix(step1): HTML 추출·제작 브리프 영상 썸네일(YouTube)+클릭 링크 (creativeThumbHtml 재사용)"
```

> preview MCP 검증 금지 — 컨트롤러가 런타임 확인(HTML 추출·제작 브리프 각 HTML에 youtube 썸네일·클릭 링크·아이콘 폴백; imageUrl/isVideo/hasImg 참조 잔존 없음).

---

### Task 3: 화면 결과 테이블 + 피로도 탭 영상 썸네일 (YouTube 배경)

**Goal(추가):** 대시보드 화면의 결과 테이블·피로도 탭에서 영상(VID) 셀이 지금은 빈 재생 박스인데, YouTube 썸네일을 배경으로 깔아 실제 미리보기가 보이게 한다. 클릭은 기존대로 모달(영상 재생) 유지.

**Files:**
- Modify: `step1_integrated.html` — `youtubeThumb` 헬퍼 신규(≈`creativeThumbHtml` 앞), `creativeThumbHtml` 내부 리팩터(regex 중복 제거), 결과 테이블 VID 셀(~5157), 피로도 탭 VID 셀(~6992).

**Interfaces:**
- Produces: `youtubeThumb(url)` → `'https://img.youtube.com/vi/{id}/hqdefault.jpg'` 또는 `''`. `creativeThumbHtml`(Task 1) 및 두 화면 테이블 셀이 공유(DRY — YouTube ID 로직 단일화).

**배경(참고):** 화면 결과 테이블(step1_integrated.html:5142-5162)은 이미지(BNR)는 실제 썸네일을 보여주지만 영상(VID)은 `<div class="preview-video">`(빨간 그라데이션 박스 + CSS `::before ▶` + `::after 'VID'`)만 표시. CSS `.preview-video`는 `position:relative` + `background:linear-gradient(135deg,#DC2828,#FF6B6B)`. 인라인 `style="background:..."`로 덮어써도 `::before ▶`·`::after VID` 배지는 요소 생성 콘텐츠라 배경 위에 그대로 표시됨. 클릭 핸들러(`openPreviewModal`/`showFatigueModal`)는 인앱 모달 프리뷰라 그대로 유지(추출물의 새 탭과 다름).

- [ ] **Step 1: `youtubeThumb` 헬퍼 추가 (creativeThumbHtml 앞)**

찾기:
```js
    // ── 소재 썸네일 셀(공용): 영상=YouTube 썸네일, 이미지=원본, 둘 다 클릭 링크(새 탭), 로드 실패 시 아이콘 폴백 ──
    function creativeThumbHtml(url, type, w, h) {
```
교체(앞에 헬퍼 삽입):
```js
    // ── YouTube 영상 썸네일 URL 추출 (watch / youtu.be / embed / shorts → hqdefault) ──
    function youtubeThumb(url) {
      const m = String(url || '').match(/(?:youtube\.com\/(?:watch\?v=|embed\/|shorts\/)|youtu\.be\/)([\w-]{11})/);
      return m ? `https://img.youtube.com/vi/${m[1]}/hqdefault.jpg` : '';
    }

    // ── 소재 썸네일 셀(공용): 영상=YouTube 썸네일, 이미지=원본, 둘 다 클릭 링크(새 탭), 로드 실패 시 아이콘 폴백 ──
    function creativeThumbHtml(url, type, w, h) {
```

- [ ] **Step 2: `creativeThumbHtml` 리팩터 (regex 중복 제거 → youtubeThumb 사용)**

찾기:
```js
      const ym = u.match(/(?:youtube\.com\/(?:watch\?v=|embed\/|shorts\/)|youtu\.be\/)([\w-]{11})/);
      const yid = ym ? ym[1] : '';
      const isImg = /^https?:\/\//.test(u) && !yid;
```
교체:
```js
      const yThumb = youtubeThumb(u);
      const isImg = /^https?:\/\//.test(u) && !yThumb;
```

찾기:
```js
      if (!yid && !isImg) return iconBox;
      const src = yid ? `https://img.youtube.com/vi/${yid}/hqdefault.jpg` : esc(u);
```
교체:
```js
      if (!yThumb && !isImg) return iconBox;
      const src = yThumb || esc(u);
```

- [ ] **Step 3: 결과 테이블 VID 셀에 썸네일 배경 (~5157)**

찾기:
```js
          previewHtml = `<div class="preview-video" onclick="openPreviewModal(${index})"></div>`;
```
교체:
```js
          previewHtml = (yt => `<div class="preview-video"${yt ? ` style="background:linear-gradient(rgba(0,0,0,.30),rgba(0,0,0,.30)),url('${yt}'),linear-gradient(135deg,#DC2828 0%,#FF6B6B 100%);background-size:cover;background-position:center;"` : ''} onclick="openPreviewModal(${index})"></div>`)(youtubeThumb(imageUrl));
```

- [ ] **Step 4: 피로도 탭 VID 셀에 썸네일 배경 (~6992)**

찾기:
```js
          previewHtml = `<div class="preview-video" onclick="showFatigueModal('${r.key.replace(/'/g, "\\'")}', '${r.유형}', '${imageUrl.replace(/'/g, "\\'")}', ${r.baselineCPA}, ${r.comparisonCPA})"></div>`;
```
교체:
```js
          previewHtml = (yt => `<div class="preview-video"${yt ? ` style="background:linear-gradient(rgba(0,0,0,.30),rgba(0,0,0,.30)),url('${yt}'),linear-gradient(135deg,#DC2828 0%,#FF6B6B 100%);background-size:cover;background-position:center;"` : ''} onclick="showFatigueModal('${r.key.replace(/'/g, "\\'")}', '${r.유형}', '${imageUrl.replace(/'/g, "\\'")}', ${r.baselineCPA}, ${r.comparisonCPA})"></div>`)(youtubeThumb(imageUrl));
```

- [ ] **Step 5: 정적 검증**

Run: `cd /c/claude/cloop_dashboard && grep -n "function youtubeThumb\|const yThumb = youtubeThumb\|youtubeThumb(imageUrl)" step1_integrated.html`
Expected: `youtubeThumb` 정의 1 + `creativeThumbHtml` 내 `yThumb` 1 + 화면/피로도 셀 `youtubeThumb(imageUrl)` 2 = 총 4줄. 구 `const ym = u.match`·`const yid = ym`는 사라져야 함(리팩터됨).

- [ ] **Step 6: 회귀 확인(pytest)**

Run: `cd /c/claude/cloop_dashboard && PYTHONUTF8=1 PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m pytest -q`
Expected: `114 passed`.

- [ ] **Step 7: 커밋**

```bash
cd /c/claude/cloop_dashboard && git add step1_integrated.html && git commit -m "feat(step1): 화면 결과 테이블·피로도 탭 영상 YouTube 썸네일 (youtubeThumb 공용화)"
```

> preview MCP 검증 금지 — 컨트롤러가 런타임 확인(결과 테이블 zeus VID 행의 `.preview-video`에 `url('https://img.youtube.com/vi/…')` 배경, ▶/VID 배지·모달 클릭 유지, 이미지 행 불변; `youtubeThumb` 무효 URL→'' graceful).

---

## Self-Review

**1. 요구 커버리지:**
- 영상 미리보기 조회 안 됨 → YouTube watch URL을 `img.youtube.com/vi/{id}/hqdefault.jpg`로 변환(헬퍼). ✅
- 썸네일 어려우면 아이콘 → 로드 실패/URL 없음 시 아이콘 폴백(`onerror`+뒤 라벨, URL 없으면 iconBox). ✅ (단 영상은 실제 썸네일 우선 — 사용자 승인)
- 이미지·영상 모두 클릭 링크 → `<a target="_blank" rel="noopener">`(영상=watch, 이미지=원본). ✅
- 세 추출 전부 → Task 1(요약) + Task 2(HTML 추출·제작 브리프). ✅

**2. Placeholder 스캔:** TBD/TODO 없음. 모든 코드 스텝 완전한 코드. ✅

**3. 타입 정합성:**
- 헬퍼 시그니처 `creativeThumbHtml(url, type, w, h)` — 네 호출부 인자 일치(문자열 url, 문자열 type, 정수 w/h). ✅
- moverRow/topHtml는 이제 `<a>`/`<div>`를 직접 반환(기존 `<div class="tt">` 래퍼 제거) — `.tr` flex 행의 첫 자식으로 `flex:none` 고정폭 유지. ✅
- previewCell 교체 시 `imageUrl`/`isVideo`/`hasImg` 제거 — 해당 행 나머지는 `v.*`·`c.유형`·`c.key`만 사용(previewCell 전용 변수라 안전). ✅
- `onerror`는 `this.style.display='none'`만(HTML 문자열 재조립·따옴표 이스케이프 없음). ✅

## Out of Scope (YAGNI)

- `이미지링크`/`meta['링크']` 데이터 소스 자체 변경(렌더만 수정).
- 비-YouTube 영상 호스트(Vimeo 등) 썸네일 — 현재 데이터는 YouTube. 비대응 URL은 아이콘 폴백으로 graceful.
- `.tt` 미사용 CSS 제거(요약 리포트) — 무해한 dead CSS, 렌더 영향 없음(최종 리뷰 트리아지 대상).
- 결과표 인라인 미리보기(대시보드 화면) 변경 — 추출물 HTML만 대상.
