# 라이브 대시보드 — 데이터 기준 표기 + 미집행 구분 설계

**작성일:** 2026-07-02
**대상:** `live_dashboard.html`, `js/live-dashboard.js`
**요구:** 라이브 대시보드가 **어떤 데이터 기준**(무슨 지표·매체 합산·기간)으로 표시되는지 명확히 표기 + 미집행 소재를 그리드에서 구분(Step 1 개념 확장).

## 현황 (조사 확정)

- 데이터 = `public/data/{title}.json`의 `kpi_daily`(GA)+`mmp_daily`(MMP) ([live-dashboard.js:375](js/live-dashboard.js)).
- **획득(acq) = GA `conversions` + MMP `installs` 합산** ([js:33-54](js/live-dashboard.js)). 노출/획득/CPI 토글.
- 기간 = 필터(전체/7/14/28일 or date) — `LIVE.state.filters.start/end`.
- 렌더 오케스트레이터 = `LIVE.render()`([js:389](js/live-dashboard.js)) → `renderChart`+`renderGrid`.
- 그리드 = 전체 소재 표시, 배지 "획득 N"([js:298](js/live-dashboard.js)). **미집행 구분 없음**.
- **표기 부족**: GA+MMP 합산 안내 없음 · 비중복 가정 미공시 · 현재 기간 요약 없음 · 미집행 구분 없음.

## 설계

### ① 데이터 기준 배지 (신규 `#liveDataBasis`)

`live_dashboard.html`의 `#liveFilters` div 닫힘(68) 다음, 차트 카드(70) 앞에 삽입:
```html
    <div id="liveDataBasis" style="margin:10px 0 0;padding:8px 12px;background:#eff6ff;border:1px solid #bfdbfe;border-radius:8px;font-size:12px;color:#1e40af;line-height:1.6;"></div>
```

`js/live-dashboard.js`에 `LIVE.renderDataBasis()` 신규 + `LIVE.render()` 최상단에서 호출:
```js
  LIVE.renderDataBasis = function () {
    const box = document.getElementById('liveDataBasis'); if (!box) return;
    if (!LIVE.state.hasKpi) { box.style.display = 'none'; return; }
    box.style.display = '';
    const f = LIVE.state.filters || {};
    let period;
    if (f.start && f.end) period = `${f.start} ~ ${f.end}`;
    else {
      const ds = LIVE.applyFilters().map(r => r.date).filter(Boolean).sort();
      period = ds.length ? `${ds[0]} ~ ${ds[ds.length - 1]} (전체)` : '전체';
    }
    box.innerHTML = `📊 <strong>데이터 기준</strong> — 획득 = <strong>Google Ads 전환 + MMP 설치</strong> 합산 · 노출/CPI는 각 매체 원값 · 기간 <strong>${period}</strong> `
      + `<span title="Google Ads와 MMP를 같은 기간 동시 집행하면 동일 유저가 양쪽에 잡혀 중복 집계될 수 있습니다. 현재 타이틀은 집행 시기가 겹치지 않아 무관합니다." style="cursor:help;border-bottom:1px dotted #1e40af;">ⓘ 합산 주의</span>`;
  };
```
`LIVE.render()`(389) 최상단에 `LIVE.renderDataBasis();` 추가.

### ② 미집행 소재 구분 (renderGrid)

`renderGrid`([js:290-305](js/live-dashboard.js))에서 소재별 셀(acq+impr)을 보고 **선택 기간에 노출·획득 모두 0이면 미집행**으로 딤+배지:
```js
  LIVE.renderGrid = function () {
    const agg = LIVE.aggregate(LIVE.applyFilters());
    const cellOf = c => agg.perCreative.get(c) || { acq: 0, impr: 0 };
    const acqOf = c => cellOf(c).acq;
    const list = [...LIVE.state.creatives].sort((a, b) => acqOf(b) - acqOf(a));
    const grid = el('liveGrid');
    if (!list.length) { grid.innerHTML = '<div class="live-empty">이 타이틀에 소재가 없습니다.</div>'; return; }
    grid.innerHTML = list.map((c, i) => {
      const cell = cellOf(c);
      const idle = LIVE.state.hasKpi && cell.impr === 0 && cell.acq === 0;   // 선택 기간 미집행
      const badge = !LIVE.state.hasKpi ? ''
        : idle ? `<div class="live-card-badge" style="background:#6b7280;">미집행</div>`
        : `<div class="live-card-badge">획득 ${cell.acq.toLocaleString()}</div>`;
      const dim = idle ? 'style="opacity:.5;"' : '';
      return `<div class="live-card" ${dim} data-idx="${i}"><div class="live-card-preview">${livePreviewHtml(c)}</div><div class="live-card-body"><div class="live-card-name">${escapeHtml(c.소재명 || c.creative_id || '')}</div>${badge}</div></div>`;
    }).join('');
    LIVE._gridList = list;
    grid.querySelectorAll('.live-card').forEach(card => card.addEventListener('click', () => {
      LIVE.openModal(LIVE._gridList[+card.dataset.idx]);
    }));
  };
```
- 미집행 = `hasKpi && impr===0 && acq===0`(선택 기간). 딤(opacity .5) + 회색 "미집행" 배지. 클릭·모달은 그대로(분석 가능).
- `hasKpi=false`(타이틀 전체 KPI 없음)이면 배지 없음(기존 유지, 미집행 표식도 안 붙임 — 전 소재가 그런 상태라 무의미).

### 캐시 버전 쿼리
`live_dashboard.html`의 `js/live-dashboard.js?v=20260626b` → `?v=20260702a`.

## 불변

- 획득 합산·필터·그래프·집계 로직 무변경. **표시(배지)만 추가.**
- 미집행 딤은 표시 전용 — 그래프/집계/정렬에 영향 없음(정렬은 기존 acq desc 유지).

## 검증 (preview)

`live_dashboard.html?title=zeus&v=20260702a&_=<ts>` 로드 후:
1. `#liveDataBasis`에 "데이터 기준 — 획득 = Google Ads 전환 + MMP 설치 합산 · 기간 …" + ⓘ 존재.
2. 기간 프리셋(최근 7일 등) 변경 시 배지 기간 갱신.
3. 그리드: 노출·획득 0 소재 = opacity .5 + "미집행" 배지. 데이터 있는 소재 = "획득 N".
4. 콘솔 error 0. 스크린샷.
