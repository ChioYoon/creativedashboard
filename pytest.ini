[pytest]
# scripts/ 의 test_*.py 는 pytest 테스트가 아니라 단독 실행 검증 스크립트
# (모듈 레벨 assert/print, def test_ 없음)라 수집 대상이 아니다.
# 정식 테스트 스위트는 tests/ 뿐 → 수집을 tests/ 로 한정.
# (없으면 scripts/·tests/ 의 동일 basename 파일이 import 충돌로 수집 실패)
testpaths = tests
