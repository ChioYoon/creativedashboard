# Com2uS R-Team CLOOP Pipeline - Stage 2 MVP dependencies
# Python 3.11+ recommended (tested with 3.14)
#
# Install:
#   pip install -r requirements.txt
# Or via setup script:
#   .\scripts\setup.ps1

# Gemini SDK (official) - 2.x is current stable
google-genai>=2.0.0,<3.0.0

# Pydantic v2 data validation
pydantic>=2.9.0,<3.0.0

# .env loader
python-dotenv>=1.0.0,<2.0.0

# CLI progress bar
tqdm>=4.66.0,<5.0.0

# Stage 5: Google Ads API
# NOTE: Python 3.14 requires google-ads >= 28.2.0 (older versions pinned to <3.13/<3.14).
# Code uses only stable surface (load_from_storage, get_service, search_stream,
# list_accessible_customers) which is unchanged from v22+ through v31.
google-ads>=28.2.0,<32.0.0
# OAuth flow helper (used only by scripts/setup-google-ads.ps1)
google-auth-oauthlib>=1.2.0,<2.0.0

# Stage 5-H: 등록부 xlsx 파싱 (registry.py)
openpyxl>=3.1
